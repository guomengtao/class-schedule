# class-schedule 质检报告 · 第 10 轮（无变更确认报告）

- **时间**：2026-09-12 18:53 (+0800)
- **本轮 HEAD**：`29c4178`（v1.4.202 / versionCode 702）
- **上一轮 HEAD**：`012343d`（第 9 轮）
- **变更量**：**零代码变更** —— `012343d..29c4178` 之间只有一个 commit `29c4178`，内容为本轮之外的 `docs(qa):` 报告文档。远端 `main` 与本轮起点完全一致，作者自第 9 轮复核后**未推送任何代码**。
- **结论性质**：本轮为**无变更确认轮**，走查与复核全部基于 `29c4178` 当前代码树重新执行（不直接抄录上轮结论）。

---

## 一、本轮结论摘要

> **一句话**：代码零变更，走查 10 条「**不通 = 0**」，**P0 连续第 2 轮保持清零**；上轮 2 条新增 P1（P1-9 / P1-10）**均未修复**，且本轮复核把 P1-9 的影响面从 3 页**扩大到 4 页**（新查出 `chinese-input-full` 同样缺 rect）。
>
> **关键风险**（按修起来最划算排序）：
> 1. **P1-10**（一行修）：输入法 `this` 丢失 → 方屏键盘横向偏移。本轮进一步收敛影响面：**9 Pro 屏宽恰好 336×480，默认值命中，9 Pro 不受影响；风险集中在 10 Pro 与其他 rect 设备**。
> 2. **P1-9**（4 页补 media）：4 个页面只有圆屏适配，缺 rect / pill，9 Pro / 10 Pro 方屏下布局错乱，入口在实验室。

---

## 二、用户操作链路走查（每轮必做 · 本轮重跑）

| # | 链路 | 结论 | 代码定位 | 推断的实际结果 |
|---|---|---|---|---|
| 1 | 首次启动 → 引导/初始化 → 进入主界面 | ✅ 通 | `welcome.ux:37 targetPage:"index-full"`、`:38 autoSeconds:3`、`:80-96` 倒计时、`:67-70` 读设置 | 3 秒后进标准页；设置里可选「手动进入」（`autoSeconds === -1`）与 `week-view` 作为默认首页，均能生效 |
| 2 | 激活状态判断（未激活 / 已激活各看到什么） | ✅ 通 | `activation.ux:435` 原生二维码 + `lib/qrcode.js`；激活码手输 | 全仓 `fetch(` = **0**，激活走本地校验 + 手输，不依赖网络，链路闭合 |
| 3 | 新增课程 → 填写 → 保存 → 下次打开还在不在 | ✅ 通 | `add-course.ux:509` `database.insertCourse` → `:511` toast「课程已添加」→ `:512` `router.back()`；`:515` 失败也有 toast | 走 database 层而非直写 storage（直写 `allCourses_` = **0 处**），`insertCourse` 内部自带 `invalidateCache` → **新增后首页立即显示**，冷启动也在 |
| 4 | 编辑已有课程 → 修改 → 保存 → 是否真的生效（历史重点） | ✅ 通 | `detail.ux:543 updateCourse` → `:545/:549` 空值拦截 → `:561 database.updateCourse` → `:563/566` 成功/失败双 toast；`database.js:291/309-313` `hit` 标记 | **上轮 P0-10「假成功」已修到位**：id 未命中时走 `formatError` → `:566` 明确弹「更新失败」，不再静默 `router.back()`。备注 `notes` 读回 `:379` / 回写 `:559` 双向正常 |
| 5 | 删除课程 → 是否真的删除、有没有确认 | ✅ 通 | `detail.ux:571 deleteCourse`；按钮 `:6`/`:115` 文本 `deleteConfirm ? '确认' : '删除'`；`database.js:339/355-359` `hit` | **两段式确认**（第一次点变「确认」，再点才删），未命中 → `:590` 弹「删除失败」，无误删、无假成功 |
| 6 | 本地存储 → 读回 → 顺序/时间/周次是否正确 | ✅ 通 | `database.js:153-156` 缓存 + `invalidateCache` 9 处（`:482/490/498/511/521` 等写操作全覆盖） | 写入后缓存失效，读回为最新；`getAllCoursesStorageWithIndex` 按 index 隔离，课表之间不串数据 |
| 7 | 主界面渲染：今天的课 / 周次切换 / 跨天 | ✅ 通 | `index-full.ux:207 getAllCourses`、`:223 onShow` 重载、`:253-255` 带 index 回读 + fallback、`:49/52/56` 加载中/失败/空三态齐全 | 从详情页返回触发 `onShow` 重新拉数据；异常时有「数据加载失败」文案兜底 |
| 8 | 设置项修改 → 保存 → 重启后是否保留 | ✅ 通 | `settings.ux` `applyTheme`→`store.setTheme`、`setSize`→`setBaseFontSize` | **即时落盘**，底部「保存」按钮属冗余但无害。重启后保留 |
| 9 | 数据同步/刷新（对接后端） | ➖ 不适用 | 全仓 `fetch(` = **0** | 手环端 `system.fetch` / `system.request` 在 9/9 Pro/10/10 Pro 全系 Not supported，本应用 100% 本地数据，无同步链路 |
| 10 | 各页面入口按钮逐个点名 | ✅ 通（29/29） | 111 处静态跳转目标全部命中 manifest 29 页；实验室 22 个入口 URI 全部有效 | 无跳错页、无死按钮。`index-full.ux` 中 `goToClassDetail` / `openPinnedPage` 等方法由 `modules/*.js` 运行时注入，非缺陷（第 4 次确认，勿再报） |

**小结：通 9 条 / 不通 0 条 / 不适用 1 条。**

---

## 三、缺陷清单

### P1 级（界面与显示 · 手环端顶格）

| 编号 | 优先级 | 模块 | 标题 | 复现步骤（用户视角） | 期望 | 实际 | 影响机型 | 严重度 | 定位 | 修复建议 |
|---|---|---|---|---|---|---|---|---|---|---|
| **P1-9** | P1 | 实验室 4 页 | **4 个页面只有圆屏适配，缺 rect / pill**（本轮由 3 页扩大为 4 页） | 实验室 → 进入「周网格(demo)」/「周网格(简)」/「周文本(简)」/「全屏中文输入」 | 方屏下按方屏布局正常显示 | 只命中 `@media (shape: circle)` 分支，方屏走默认样式 → 边距/尺寸沿用圆屏方案，9 Pro / 10 Pro 上布局错乱或内容超出 | **9 Pro、10 Pro**（rect） | 中高 | `week-grid-demo.ux:247`、`week-grid-simple.ux:183`、`week-text-simple.ux:251`（三者 rect/pill **均无**）；`chinese-input-full.ux`（**本轮新查出：有 pill 但缺 rect**） | 参照 `settings.ux` 补 `@media (shape: rect)` 与 `@media (shape: pill-shaped)` 两段 |
| **P1-10** | P1 | 输入法组件 | **`adjustScreenWidth` 回调 `this` 丢失 → 屏宽恒为 336**（第 9 轮 ES5 改造引入的回归） | 方屏设备上打开昵称编辑 / 全屏中文输入，唤起键盘 | 键盘按真实屏宽居中 | `success: function(data){ this.screenWidth = ... }` 中 `this` 非组件实例 → 赋值无效，`screenWidth` 恒为默认值 → 键盘横向居中偏移 | **10 Pro** 及其他非 336 宽 rect 设备；**9 Pro（336×480）恰好命中默认值，不受影响** | 中 | `InputMethod.ux:808-817`（`:811` / `:814`）；调用点 `:456-457`（`screentype==='rect'\|\|'pill-shaped'` 时触发，确会被调用） | 一行修：`var self = this` 后回调内改 `self.screenWidth`，或改回箭头函数 |
| **P1-3** | P1 | 6 个弹层页 | 圆屏 modal 四角裁切 | 圆屏手表打开 modal | 四角不被裁 | 已补 `@media (shape: circle)` modal 宽度 72% + overlay padding | 圆屏设备 | 中 | 修复已合入（`e12a13c`） | ⏸ **待真机验证**（已问 2 轮） |
| **P1-4** | P1 | 全局 | `designWidth: device-width` 写死 px，无机型折算 | 不同分辨率方屏 | 自适应 | 固定 px，无 `screenWidth` 折算 | 10 Pro（分辨率与 9 Pro 不同） | 中 | `manifest.json` `config.designWidth` | ⏸ 作者已接受但未排期（已问 3 轮） |
| **P1-8** | P1 | 全局 | 零遥测，线上问题无感知 | 用户遇到崩溃 | — | 无上报通道 | 全部 | 低 | — | 挂账。**注意：手环不能联网，不要按「加 fetch 上报」做**；可行方案为本地错误环形队列 → 诊断页二维码 → 手机扫码上报 |

### P2 级（输入与交互）

| 编号 | 优先级 | 模块 | 标题 | 定位 | 说明 |
|---|---|---|---|---|---|
| **P2-11** | P2 | 欢迎页 | **进入按钮无防重入锁**（本轮精确根因：前哨变量还在，但**只写不读**） | `welcome.ux:53`（`onShow` 里 `_hasEntered=false`）、`:119-131 doEnter`、`:132-144 enterSchedule` | 两个进入方法都在 `router.replace` 的 **success/fail 回调里写** `_hasEntered`，但**进入前从不 `if (this._hasEntered) return`** → 连点两次会连发两次 `replace`，页面栈异常。修法：方法首行加 `if (this._hasEntered) return`，并在进入前先置 `true`（而非回调里置） |
| **P2-12** | P2 | 课程详情 | 参数缺失仍 `selectCourse(0)` 兜底 | `detail.ux:353-355` | 不丢数据（P0-10 已堵死假成功），仅表现为「打开的是第 0 门课」的困惑体验 |

### P3 级（代码质量）

| 编号 | 优先级 | 模块 | 标题 | 定位 |
|---|---|---|---|---|
| **P3-13** | P3 | 配置 | `manifest.json` 缺文件末尾换行 | `src/manifest.json`（本轮复核：结尾字节为 `7d`，确无 `\n`） |
| **P3-14** | P3 | 欢迎页 | `_hasEntered` 只写不读（与 P2-11 同源） | `welcome.ux:53/125/128/141/144` |

### 本轮新增检查项（结论：✅ 无新缺陷）

按第 9 轮约定，本轮新增「`require("@system.*")` 清单 vs `manifest.json` features 是否对齐」检查：

| require 的 feature | 次数 | manifest 是否声明 |
|---|---|---|
| `@system.storage` | 52 | ✅ `system.storage` |
| `@system.router` | 10 | ✅ `system.router` |
| `@system.prompt` | 4 | ✅ `system.prompt` |
| `@system.device` | 2 | ✅ `system.device` |
| `@system.resident` | 1 | ✅ `system.resident` |
| `@system.app` | 1 | ✅ `system.app`（第 9 轮 `852a256` 补齐） |

**6/6 全部声明，无遗漏。** 反向：manifest 中 `system.sqlite` / `system.vibrator` / `system.configuration` / `system.alarm` / `system.calendar` / `system.request` 未在代码中 require，属冗余声明，不影响使用，不计缺陷。

### ES6+ 语法扫描（老内核整段不执行风险）

- 全仓箭头函数 `=>`：**0 处** ✅（第 9 轮 `793c6dc` 清零后无回归）
- `const` / `let`：仅出现在顶层 require 与局部循环（如 `InputMethod.ux:453-457`），与其余 29 页同写法且线上运行正常，**不计风险**（沿用第 8 轮判定）
- P1-10 是本轮唯一与 ES5 改造相关的遗留问题

---

## 四、9 Pro / 10 Pro 机型适配对比

| 维度 | 小米手环 9 Pro | 小米手环 10 Pro |
|---|---|---|
| 屏幕形态 | 方屏 `rect` | 方屏 `rect` |
| 分辨率 | **336×480**（仓库 `docs/index-band9pro-black-screen-analysis.md:148` 实证） | 与 9 Pro 不同（作者文档 `docs/qa-round1-report.md:110` 记录） |
| 顶部安全区 `padding: 44px` | ✅ 29/29 页覆盖 | ✅ 同上 |
| `@media (shape: circle)` | ✅ 29/29 | ✅ 29/29 |
| `@media (shape: rect)` | ⚠️ **25/29**（缺 P1-9 的 4 页） | ⚠️ 同左 |
| `@media (shape: pill-shaped)` | ⚠️ **26/29**（缺 3 页） | ⚠️ 同左 |
| 输入法键盘宽度 | ✅ 默认 336 恰好命中，**无影响** | ⚠️ **P1-10：恒取 336，与实际宽度不符 → 键盘偏移** |
| 圆屏 modal 裁切 | 不涉及 | 不涉及（P1-3 属圆屏设备问题） |
| 固定 px 无折算（P1-4） | 中（已知尺寸） | ⚠️ 较高（分辨率不同） |
| 返回键 `onBackPress` | ✅ `index-full.ux:357` + `@system.app` 已声明，退出正常 | ✅ 同左 |

**本轮机型结论**：9 Pro 受影响面比上轮评估更小（P1-10 不命中）；**10 Pro 是当前适配风险更集中的机型**，建议作者以 10 Pro 作为下一轮真机验证主测机。

---

## 五、与上一轮对比

| 项 | 第 9 轮（`012343d`） | 第 10 轮（`29c4178`） | 变化 |
|---|---|---|---|
| 代码变更 | 有（5 个 commit） | **无** | 零变更 |
| 走查「不通」条数 | 0 | **0** | 持平 |
| P0 数量 | 0 | **0** | ✅ 连续 2 轮清零 |
| P1-9 | 新增（3 页） | **未修，且扩大为 4 页** | 🔴 恶化（本轮多查出 `chinese-input-full`） |
| P1-10 | 新增（回归） | **未修** | 🔴 遗留 |
| P2-11 / P2-12 / P3-13 / P3-14 | 新增 | **均未修**（本轮复核确认） | 🔴 遗留 |
| require vs features 对齐 | 未检查 | ✅ 6/6 通过 | 新增检查项，无缺陷 |

### 历轮已修项回归重扫（铁律：作者改 A 时可能改回 B）

| 检查项 | 期望 | 实测 | 结论 |
|---|---|---|---|
| `deleteScheduleAndShift` 出现次数 | ≥8 | **8** | ✅ 无回归 |
| `onBackPress` | 1 | **1** | ✅ |
| `lab-module-test`（震动模块测试） | 0 | **0** | ✅ |
| `fetch(` | 0 | **0** | ✅ |
| `padding: 44px` 覆盖页 | 29/29 | **29/29** | ✅ |
| `invalidateCache` 调用 | ≥9 | **9** | ✅ |
| `database.js` `hit` 标记（P0-10 修复） | 存在 | **`:291/:309`、`:339/:356` 均在** | ✅ P0-10 未改回 |
| 直写 `allCourses_` | 0 | **0** | ✅ |
| 箭头函数 | 0 | **0** | ✅ |

**结论：历轮已修项本轮零回归。**

### 指标（同脚本 `scripts/ui-audit-schedule.py` 同口径）

| 指标 | R8(`e12a13c`) | R9(`012343d`) | R10(`29c4178`) |
|---|---|---|---|
| 行高覆盖 | 22.3% (210/942) | 22.3% | **22.3%**（持平） |
| 溢出保护页 | 13/29 | 13/29 | **13/29**（持平） |
| 无三态页面 | 8/29 | 8/29 | **8/29**（持平） |
| 热区 <40px | 184 | 184 | **184**（可点击 284，未判定 43） |
| 箭头函数 | 3 → 0 | 0 | **0** |
| `@media rect` 覆盖 | 26/29（旧口径） | 26/29（旧口径） | **25/29**（本轮精确口径，修正上轮数字） |

> 口径修正说明：本轮按「每页逐页 grep」精确统计，上轮的 26/29 未区分 `chinese-input-full` 只补了 pill 未补 rect 的情况，实际 rect 为 **25/29**。

---

## 六、回归测试建议（按用户操作路径组织）

| # | 场景 | 步骤 | 期望 | 关联缺陷 |
|---|---|---|---|---|
| 1 | 冷启动进主页 | 手环冷启动 → 观察欢迎页 → 等待或点「进入」 | 3 秒后进标准页；**连点 2 次只进入一次** | P2-11 |
| 2 | 新增课程 | 首页 → 添加课程 → 填名称/时间 → 保存 | toast「课程已添加」→ 返回首页**立刻**能看到这节课 | 历轮 P0-2 |
| 3 | 编辑课程 | 点某节课 → 改名 → 点「更新」 | toast「课程已更新」；重新打开该课，**改动与备注都在** | 历轮 P0-10 |
| 4 | 删除课程 | 点某节课 → 点「删除」（按钮变「确认」）→ 再点 | toast「课程已删除」；列表该课消失 | 历轮 P0-10 |
| 5 | 备注保留 | 编辑时填备注 → 更新 → 重开 | 备注在，不被清空 | 历轮 P0-3 |
| 6 | 设置持久化 | 设置改主题/字号 → 返回 → 重启应用 | 主题与字号保留 | — |
| 7 | 返回退出 | 首页按返回键 | 应用正常退出，不卡死 | 历轮 P0-8 |
| 8 | **9 Pro 方屏输入法** | 9 Pro 进入昵称编辑 → 唤起键盘 | 键盘横向居中、无偏移（9 Pro 屏宽 336，预期无问题） | P1-10 |
| 9 | **10 Pro 方屏输入法** | 10 Pro 进入昵称编辑 → 唤起键盘 | 键盘按实际屏宽居中 | **P1-10（重点）** |
| 10 | **实验室 4 页方屏** | 实验室 → 周网格(demo)/周网格(简)/周文本(简)/全屏中文输入 | 方屏下布局正常、内容不超出 | **P1-9（重点）** |
| 11 | 空态 | 删光所有课后看首页 / 课表管理 / 课程管理 | 显示「今天没有课」「暂无课程表」「暂无课程」 | U-10 |
| 12 | 数据库失败态 | 异常时看首页 | 显示「数据加载失败」+ 重试 | P1-5 |

---

## 七、已知风险 · 不急修（P4）

> 这一节只是「知道就行」，**不排期、不催修、不写进交接本待办表**。

| 风险 | 损失上限 | 触发条件 |
|---|---|---|
| 激活状态存本地，用户改机或重装后可重新走激活流程 | 单次授权费用（几十元级） | 用户主动改机/重装 |
| 激活码校验逻辑在包体内，理论上可被逆向本地篡改 | 同上 | 用户具备逆向能力 |

当前用户体量（几十人）下这两项的实际损失可忽略，作者定位为「算一种福利，不着急」。**本轮不新增 P4 条目。**

---

## 八、归档信息

- 本地报告：`reports/class-schedule-QA-20260912-1853.md`
- 交接本：`class-schedule/docs/qa-reports/HANDOFF/2026-09-12-round10.md`
- 状态台账：`class-schedule/docs/qa-reports/HANDOFF/STATUS.md`
- 项目云盘（tdrive）：**不可用** —— `~/.workbuddy/mcp.json` 不存在，第 10 次确认，已明确跳过，未静默略过
- 飞书通道：**已停用**（2026-09-12 用户决定），未使用
