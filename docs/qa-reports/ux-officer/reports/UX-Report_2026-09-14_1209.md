# UX 巡检报告 · 2026-09-14 12:09（第 5 轮）

> **用户体验官（UX Officer 自动巡检）**
> 仓库：guomengtao/class-schedule ｜ 分支 main ｜ HEAD `6cabb90` ｜ versionName **1.4.242** / versionCode **742** / minPlatformVersion 1000
> 上轮业务基线 `033e12c`（v1.4.231）→ 本轮 `6cabb90`（v1.4.242），**区间 19 个提交、src +1481/-1042、25 文件，属大变更轮**
> 协同：已读主 QA `HANDOFF/REPLY.md`（第 9 轮，含 `29e6f1b` UX 修复登记）与 `STATUS.md`（仍停第 11 轮 / 1.4.209，已滞后）；本报告只写 UX-x，不改动主 QA 任何文件。

---

## 一、本轮总体结论（先看这里）

**这是一轮「集中修复 + 输入法大重构」轮，用户侧关键问题显著好转，主链路零回归，未发现新增 P0。**

- 作者用 `29e6f1b fix(ux)` 一次性修掉上轮报的 **UX-17(P0)、UX-15、UX-18、UX-20、UX-16** 共 5 条；随后 `9f00b79/31c5cc7/7785cc0` 把全站中文输入**集中到单一 `chinese-input` 页、删除 `chinese-input-full`（-371 行）、8 个输入入口统一回填协议**，`feae8e2` 加回振动声明，`bfb4a86/7544892/b64539f/8e3712a` 多轮优化 add-course/course-manager 胶囊屏，并新增实验页 `lab-add-course`（720 行）。
- 我对上述修复**逐条回代码复核**：5 条全部在代码层落地（详见第二节），其中 4 条需真机最终确认。
- 对大重构做了**回归走查**：8 个输入入口「打开→输入→回填」协议统一配对、add-course/lab-add-course 保存链路前置校验+成败 toast 齐全、被删页无死路由、振动声明与调用重新一致——**未发现「输入丢失 / 保存假成功 / 点了没反应」类新 P0**。
- 主 QA 台账（第 11 轮）标红的 **P0-11 / P1-10 / P1-12 / P1-14** 与本轮 UX-17/18/20/16 同根，**建议主 QA 一并转绿**（代码证据见下）。

**仍需用户/作者处理的重点（按用户视角排序）**：① 一批「已改代码、待真机」的验证（输入法键盘偏移是重中之重）；② 调试/实验页仍全部在正式路由且本轮又净增 1 页（UX-9）；③ 字号体系：标题 38 全站仍为 0、付费弹层仍有 12px、胶囊页新引入 24/28px 档（UX-4/5/21）。

---

## 二、状态流转：已修复复核（逐条代码证据）

| 编号 | 级别 | 上轮状态 | 本轮 | 代码复核证据（HEAD 6cabb90） | 与主 QA 对照 |
|---|---|---|---|---|---|
| **UX-17** 二维码输入被 maxlen=5 卡死 | P0 | 🔴 | 🟢 **代码已修·待真机** | `qrcode-generator.ux:144` 已补 `storage.set chinese_input_maxlen="100"`；REPLY 登记触顶 showToast「已达输入上限」；onShow:109-115 读 `chinese_input_result` 回填并删除，链路完整 | 同 **主QA P0-11**，建议转🟢 |
| **UX-18** 输入法 screenWidth 回调 this 丢失、缺 watch | P1 | 🔴 | 🟢 **代码已修·待真机** | `InputMethod.ux:811-818 adjustScreenWidth` 已 `var self=this`、success `self.screenWidth=data.screenWidth`、fail 兜底 336；`:472` 补 `$watch("screentype","adjustScreenWidth")`；`:456-457` rect 与 pill-shaped 都触发；`:263` 模板新增 pill-shaped 分支；配合 `3aed24c` 在 JS 层把 capsule 映射为 pill-shaped | 同 **主QA P1-10**（其要求的 self + $watch 两点都已做），建议转🟢待真机；亦缓解 U-1 |
| **UX-15** manifest 删振动声明但代码仍调、onVibrate 无保护 | P1 | 🔴 | 🟢 **代码已修·待真机** | `manifest.json:26` 加回 `system.vibrator` feature（`feae8e2`），声明与调用重新一致；`InputMethod.ux:790-796 onVibrate` 已包 `try{vibrator.vibrate}catch(e){}`；vibratemode 收敛到 chinese-input + InputMethod 两文件 | — |
| **UX-20** 5 处固定宽度超胶囊屏 | P1 | 🔴 | 🟢 **代码已修·待真机** | welcome `.enter-btn` 胶囊块收 160px+max-width:100%（:272 区）、schedule-qrcode 二维码收窄、add-course `.course-card` 胶囊块 **140px**（:912）、detail `.course-card` 胶囊块 **160px+max-width:100%**（:980）；基础层 200/220 仅作用于 336 方屏（不溢出），合理 | 同 **主QA P1-12**，建议转🟢待真机 |
| **UX-16** 12 个二级页根块缺 width:100%、冗余 min-height | P2 | 🔴 | 🟢 **代码已修** | 原 12 页（chinese-input-full 删除后为 11 页）根块均已有 width:100%；全仓 `min-height:100%` 18→**6，且剩余 6 处全部位于 bs-demo1/bs-demo3/black-screen-check 黑屏对比调试页（故意保留），正式页清零** | 同 **主QA P1-14**，正式页部分建议转🟢（调试页残留随 UX-9 剔除） |
| **UX-10** 「删振动不彻底」 | P1 | 🔴 | ⚪ **关闭（方向变更）** | 作者最终方案为**保留按键振动**（而非删除）：声明已加回、调用已加保护、vibratemode 已收敛，「删不干净」的问题前提不再成立 | — |

### 输入法集中化重构的专项回归（本轮最大变更，重点排查）
- **死路由/死引用**：`chinese-input-full` 在 manifest 路由与全仓 .ux/.js 引用 grep **全空**，删除干净，无跳砖风险。
- **回填协议统一**：8 个调用入口（add-course、detail、course-manager、nickname-edit、schedule-manager、custom-content-edit、qrcode-generator、lab-add-course）**全部**「set return_key=chinese_input_result → onShow 读 chinese_input_result 回填 → delete」，并配套 title/placeholder/value/maxlen，协议一致。
- **排除「多字段串值」疑似点**：add-course / lab-add-course 的课程名走**预设列表点选**（非自由文本），仅「上课地点」一个字段走中文输入，故共用 result key 不会串字段。
- **保存主链路**：add-course `saveCourse`（:477 校验「请先选择课程/请先设置时间」→ :509 insertCourse → :511 成功 toast+back / :515 失败 toast）与新页 lab-add-course `saveCourse`（:408-433 同构）**均健壮，未被重构改坏**。

---

## 三、本轮新增问题

### UX-21 · 胶囊添加课程页新引入 24/28px 字号档，低于「正文 32」统一基准
- **级别**：P2（体验/一致性，胶囊优先）｜**形态**：胶囊屏优先，方屏次之｜**类型**：字号标准
- **位置**：`pages/add-course/add-course.ux` 胶囊块（:858 起）、`pages/course-manager/course-manager.ux`；`bfb4a86` 按「30/28/24px 字体、52px 按钮」改造。
- **现状（实测档位）**：add-course 胶囊块 font-size = 24px×4、28px×11、30px×2、32px×1，**主力是 28px、最小 24px**。
- **用户影响**：24/28 虽 ≥ 底线 22、不会「看不见」，但低于既定「正文 32」基准；`designWidth=device-width` 等比缩小后，添加课程这一高频页正文在米环 9（192 宽）上偏小，与「正文 32、宁大勿小」的定调存在张力。
- **修复建议**：需作者拍板胶囊正文档位——要么胶囊高频页正文回到 32（辅助才用 26/22），要么正式确认「胶囊正文允许 28、辅助 24」作为胶囊特例并写进字号规范，避免后续页面各写各的。
- **关联**：UX-4（全站字号体系）的本轮具体化；主 QA U-4（微小字号）只清到 12px，不覆盖本档。

> 说明：本轮刻意不新增「为凑数」的编号。重构后未发现确定性 P0/P1 新缺陷；新页 lab-add-course 的「实验功能进正式包」问题并入既有 UX-9（范围扩大），其 3 处空 fail 并入 UX-1 计数，不另立编号。

---

## 四、遗留问题状态（UX-1 ~ UX-20 中未关闭项）

### P1（用户可感知）
- **UX-1 空 fail 静默回调**：32 → **29 处**（-3；删除 chinese-input-full -5、nickname-edit 收敛，新增 lab-add-course +3）。主保存链路已安全，剩余为页面级读写失败无提示。集中文件：custom-content-edit、chinese-input、add-course、lab-add-course、course-manager 等。
- **UX-4 字号体系偏小/档位混乱**：<22px 299 → **276 处（-23）**；但**标题 38px 全站仍为 0、正文 32px 仅 48 处**，主力仍是 20px(103)/18px(48)/12-16px 等；本轮胶囊页又引入 24/28 档（见 UX-21）。档位分布：20px=103、18px=48、12px=28、13px=26、14px=25、16px=20、15px=13、11px=6、17px=6、19px=1。
- **UX-5 付费转化弹层小字**：`unlock-dialog` 已达标（最小 20/22）；**`pro-card` 最小仍 12px×3、`premium-overlay` 仍 12/13px**，付费决策页文字偏小，建议优先拉到底线 22 以上。
- **UX-9 调试/实验页全部在正式路由（本轮范围扩大）**：device-info、vibration-lab、lab、lab-settings、black-screen-check、bs-demo1~5、capsule-hide-test **一个未剔除**，且**本轮新增 `lab-add-course`（manifest:147，720 行，从 lab-list「添加课程(胶囊)」进入）**。新页本身代码质量良好（根块 100%、字号全 ≥22、无超宽固定值、保存链路完整），但属未完成实验功能，随正式包发布增大体积与用户误入概率。
- **UX-7 无效 `(shape: capsule)` 残留**：37 → **36 处 / 34 文件**（-1）；合法 pill-shaped 44 处。当前多以 `(shape:capsule),(shape:pill-shaped)` 并列书写、靠合法值命中，建议直接删非法 capsule 值，避免误导。

### P2
- **UX-2 预设/演示数据硬编码**（定位修正）：原 course-manager 快速添加占位已随重构消失；现仅剩 `data/database.js:624-635 _doResetToDemoData` 的 12 门示例课写死「王老师/301教室」等。用户「恢复演示数据」后得到一套像真课的假教师/教室，需逐个改，建议明确标注「演示数据」或留空教师/教室。
- **UX-3 添加返回刷新**：代码层具备刷新链（index-full onShow refreshClasses），维持 🟡 待真机走「添加→保存→返回」闭环。
- **UX-6 顶栏时钟/星期字号偏小**：`components/clock` clock-text 仍 **18px**、index-full clock-text 20px、day-nav-text **18px**，均低于底线 22，建议 22~26。
- **UX-8 显隐用 if 销毁为主**：`if="{{` 125 → **129（+4）**、display:none 仍仅 2 处；结合历史局部黑屏前科，建议暂隐优先 display:none。
- **UX-19 胶囊设置页隐藏全部栏目说明**：settings 胶囊块（:803，已并列合法 pill-shaped，块可命中）:832-833 仍 `.row-hint{display:none}`，胶囊屏「栏目时间/云端备份/实验性功能/胶囊屏适配/解锁/支持/诊断/重置」等 11 条说明全不可见。主 QA P1-15 担心的 flex 挤压在当前结构下已不存在，现状是**作者主动选择隐藏**——需拍板：保持隐藏，还是把关键说明精简为底线 22px 单行。
- **UX-11 包体积余量**：src 仍约 **1.5M**（删 371 行中文页、增 720 行实验页，净变化不大），距 2MB 红线余量有限；剔除 UX-9 调试/实验页可释放。

### P3（简列，不展开）
- **UX-12** console.* 142 处（持平），backup-restore 30 最多，发布前建议降 error 级。
- **UX-13** 根目录临时脚本（fix_capsule_*.py / audit_capsule.py / capsule_audit.txt 等）发布前清理。
- **UX-14** minPlatformVersion=1000 偏低，建议抬高或用 JS 宽高比兜底。

---

## 五、量化趋势（基线 → 上轮 → 本轮）

| 指标 | 09-13 基线 | 09-14 06:17 | 09-14 12:09 | 本轮变化 |
|---|---|---|---|---|
| 业务 HEAD / 版本 | f8f8920 / 1.4.217 | 033e12c / 1.4.231 | **6cabb90 / 1.4.242** | 大变更（19 提交） |
| 空 fail 回调 | 31 | 32 | **29** | -3 |
| <22px 字号 | 347 | 299 | **276** | -23 |
| 标题 38px / 正文 32px | 0 / 47 | 0 / 45 | **0 / 48** | 标题档仍缺位 |
| 无效 shape:capsule（处/文件） | 33 | 37/35 | **36/34** | -1 |
| `if="{{` / display:none | 123 / 2 | 125 / 2 | **129 / 2** | if +4 |
| min-height:100%（正式页/全仓） | — | 12 / 18 | **0 / 6**（余者全在调试页） | 正式页清零 |
| import vibrator / vibratemode 文件 | — | 3 / 4 | **3 / 2** | 收敛 |
| console.* | 140 | 142 | **142** | 持平 |
| 调试/实验页在正式路由 | 全在 | 全在 | **全在且 +lab-add-course** | 未剔除反增 |
| src 体积 | ≈1.5M | ≈1.5M | **≈1.5M / 138 文件** | 持平 |
| UX 编号累计 | UX-1~14 | UX-1~20 | **UX-1~21**（本轮 +1，UX-10 关闭） | — |

---

## 六、修复优先级顺序（建议作者照此处理）

1. **【先真机验证，不写新代码】** 米环 9/10 胶囊真机回归输入法（对应 UX-18 / 主QA P1-10、U-1）：连续中文输入候选栏不偏移、点选准确；顺带验证 UX-17 二维码可超 5 字、UX-15 按键振动正常、UX-20 首屏/卡片不溢出、UX-3 添加后返回首页立即可见。这是本轮所有「🟢待真机」项的一次性验收。
2. **UX-5** 付费弹层 pro-card / premium-overlay 的 12/13px 拉到 ≥22（付费转化页，改动小、收益直接）。
3. **UX-9** 正式构建剔除调试/实验页（含本轮新增 lab-add-course、bs-demo1~5、black-screen-check、capsule-hide-test、vibration-lab、device-info、lab-settings），同时消解残留 min-height、释放包体积（UX-11/UX-13）。
4. **UX-6** 时钟/星期 18-20px → 22~26。
5. **UX-1** 剩余 29 处空 fail 补 showToast；**UX-7** 删除非法 capsule 媒体值只留 pill-shaped。
6. UX-4/UX-21 字号按拍板结论分批收敛；UX-2 演示数据标注；UX-8 暂隐改 display:none；P3 随发布前清理。

## 七、待作者拍板项
1. ~~振动保留还是删除~~ → **已落地「保留」（加回声明 + try/catch），UX-10/15 关闭，无需再决策**。
2. **调试/实验页正式构建是否统一剔除？**（本轮又多 1 个 720 行实验页，决定 UX-9/UX-11/UX-13，建议尽快定）
3. **胶囊正文字号档位**：是否允许用 24/28px 替代正文 32（UX-21）？全站标题 38px 何时补（UX-4）？付费弹层是否先改（UX-5）？
4. **胶囊设置页说明文字**（UX-19）：保持隐藏，还是精简为 22px 单行显示关键项？

## 八、待真机验证清单（大重构后建议逐项打勾）
- [ ] 8 个中文输入入口逐个「打开→输入→回填」正确（重点昵称、重命名课程/课表、二维码、上课地点）
- [ ] 米环 9/10 T9 九键连续输入候选栏居中、不右偏、可左右滑（UX-18）
- [ ] 二维码文本可输入 >5 字、达 100 字上限有提示（UX-17）
- [ ] 按键振动正常、无报错弹窗（UX-15）
- [ ] 胶囊屏 welcome 主按钮、add-course/detail 课程卡片、二维码不溢出（UX-20）
- [ ] 添加课程→保存→返回首页/课表立即出现，无需重启（UX-3）

---
*本报告由用户体验官（UX Officer）每 6 小时自动生成；只做静态走查，运行时现象均标注「待真机」，不臆断为确定 bug。下轮将重点复核本轮「待真机」项是否被作者回填、以及 UX-5/9 的处理进展。*
