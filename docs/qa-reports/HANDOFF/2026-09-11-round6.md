# QA 交接本 · round 6（作者登记处理轮）

- **日期**：2026-09-11 23:25
- **本轮 HEAD**：`cb94d64`（版本 1.4.193）
- **类型**：作者 `REPLY.md` 三区登记 → 逐条搬运 + 代码复核
- **台账**：`HANDOFF/STATUS.md`（已同步更新）
- **作者登记原文**：`HANDOFF/REPLY.md`（2026-09-11 23:15 首次填写）

---

## 一、本轮发生了什么

作者**首次填写 `REPLY.md`**（此前 round1~round5 回复区空挂 5 轮）。三区登记内容：

| 区 | 作者登记 |
|---|---|
| ① 已处理 | P0-1、P0-4、P0-5、P0-6、P0-7、P0-8、P0-9（共 7 条，commit 均标 `cb94d64`） |
| ② 处理中 | P0-2、P0-3（计划下次提交） |
| ③ 打算做 | P1-1/1b、P1-2、P1-3、P1-4、P1-5、P1-6、U-2、U-4、U-5、U-6、U-7、U-8、U-9、U-10、U-11、U-12（共 16 条） |
| ③ 不打算做 | U-1（需真机联调）、U-3（临时设计） |

---

## 二、⭐ 本轮最重要产出：7 处登记与代码不符

按「文档会过期，只信代码」铁律逐条复核，发现作者**低估了自己的修复量**：

### A. 作者以为没修，实际 `cb94d64` 已修好（5 条）

| 编号 | 作者登记 | 代码实际 |
|---|---|---|
| P0-2 | ② 处理中（下次修缓存） | `add-course.ux:505` 已走 `database.insertCourse`；直写 `storage.setItem` **0 处**、`allCourses_` **0 处**；`database.insertCourse` 内部已有 `if (!err) invalidateCache(currentScheduleIndex)` → 缓存问题一并解决 |
| P0-3 | ② 处理中（下次改 `notes: ""`） | `detail.ux:379` `self.courseNotes = c.notes \|\| ""` 读原值；`:559` `notes: this.courseNotes \|\| ""` 回写 → 硬编码已消失 |
| P1-1 | ③ 打算做（补 @media） | `@media (shape:...)` **29/29 全覆盖，0 页缺失** |
| P1-5 | ③ 打算做（加错误提示） | `index-full.ux:52` 已有「数据加载失败」+ 重试 ×1 + error ×6 |
| U-12 | ③ 打算做（去重） | `pages/countdown-demo/` **目录已删除** |

**影响**：若照登记表执行，作者会在已修好的 5 条上重复投入。已在台账「差异清单 A 区」标红。

### B. 作者标「不打算做」，但理由不成立（2 条，我不关闭）

| 编号 | 作者理由 | QA 判定 |
|---|---|---|
| U-1 | 「需真机联调，无法静态修复」 | 同一份表「真机现象回填」写的是「**还没上真机，待测试后回填**」→ 是**没测**不是**不修**。标 ⚪ 会让这条永久消失，而作者明确说要测 → 维持 **⏸ 挂起（已问 3 轮）** |
| U-3 | 「临时设计，后续考虑全 App 铺开」 | 实际是「当前有意、后续要做」= ③ 打算做 → 改判 **🔴 未修（低优先）**，备注作者答复，**解除卡住**，不再每轮追问 |

### C. 计划与现状不一致（1 条）

| 编号 | 作者计划 | 代码现状 |
|---|---|---|
| P1-6 | 详情页参数改为**路由参数** | 仍走 `storage` 传 `detail_classId`/`detail_day`，**未改路由参数**；但已加 `fail` 回调 → 失败 `showToast` 且**不再 push**。「编辑到上一门课」危害已消除 → 保持 🟢，路由改造留作可选优化 |

---

## 三、本轮待办（P0 / P1）

| 编号 | 标题 | 优先级 | 状态 | 定位 | 建议顺序 |
|---|---|---|---|---|---|
| U-7 | 方屏（9 Pro/10 Pro）按钮热区 28×24px | P1 | 🔴 未修 | `statistics.ux` `@media (shape:rect)`；全仓 <40px 按钮 158 个 | **1（改几个数字，收益最高）** |
| U-2 | T9 键盘缺 `jp.png` | P1 | 🔴 未修 | `components/InputMethod/assets/t9/` | **2（补一个文件）** |
| U-8 | 4 处英文 `back` 未统一 | P1 | 🟡 部分改善 | 全仓，文案 7 种 → 5 种 | **3（全局替换）** |
| U-9 | 彩色 emoji 当图标 | P1 | 🔴 未修 | `data/store.js` THEMES icon | 4 |
| U-10 | 12 个页面缺三态 | P1 | 🔴 未修 | 全仓 | 5 |
| U-4 / U-5 / U-6 | 微小字号 / 行高 / 溢出保护 | P1 | 🟡 部分改善 | 全仓 | 6（批量 CSS） |
| U-11 | 双轨配色 | P1 | 🟡 部分改善 | `detail.ux` / `add-course.ux` `<style>` | 7 |
| P1-2 / P1-3 | 安全区 / 圆屏四角 | P1 | 🟡 部分改善 | 全仓 | 8（真机确认） |
| P1-4 | 全站写死 px | P1 | 🔴 未修 | `manifest.json` `designWidth` | 9（**可能本就不算缺陷，待确认后关闭**） |
| U-3 | 字体设置范围 | P1 | 🔴 低优先 | 已答复：临时设计 | 10（不催） |
| U-1 | 输入法定位 | P1 | ⏸ 挂起 | `InputMethod.ux` 78 处绝对定位 | —（等真机） |

**P0 全部清零，本轮无 P0 待办。**

---

## 四、真机回归 checklist（给作者，10 分钟）

在 **9 Pro 与 10 Pro 各走一遍**：

- [ ] 新增课程 → 保存 → 返回首页 → **课程立即可见**（不用重启）
- [ ] 编辑已有课程 → 改备注 → 保存 → 重新打开 → **备注还在**
- [ ] 删除课表 → 确认 → **剩余课表不串表**
- [ ] 首页按返回键 → **能退出应用**
- [ ] 切到 T9 日文科 → 看图标**是否裂图**（U-2）
- [ ] 方屏（9 Pro）进统计页 → 点返回键 → **是否好点**（U-7）
- [ ] 唤起输入法 → 记录现象 **A 唤不起 / B 偏右被切 / C 能出打不出字**（U-1）

---

## 五、作者回复区

> **请填 `REPLY.md`（唯一入口），不要改本文件。**

| 编号 | 你的结论 |
|---|---|
| P0-2 / P0-3 / P1-1 / P1-5 / U-12 | （已实际修复，请确认是否认可 QA 判定，认可则可从你的待办划掉） |
| U-1 输入法 | 真机现象：A / B / C / 真机没问题 |
| U-7 方屏热区 | 改 / 不改（理由） |
