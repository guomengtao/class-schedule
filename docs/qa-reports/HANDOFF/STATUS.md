# 缺陷总台账（唯一权威状态表）

> **看这一张表就够了。** 每一轮质检后由 QA 更新，作者**只读**。
> 作者那边对应的是 `REPLY.md` 的**三区登记**（① 已处理 / ② 处理中 / ③ 待处理）—— 两边用同一套编号，可以直接对着看。
> 初始化：2026-09-11（round3 功能 + round4 UI）｜最近更新：**2026-09-12 12:44 QA 第 9 轮**｜上一轮：**2026-09-12 06:16 QA 第 8 轮**｜HEAD **`012343d`**（版本 **1.4.202**）

## 🟢 当前结论：P0 全部清零；P0-10 / P1-7 已修并复核；新增 2 条 P1

本轮 `e12a13c..012343d` **有真实代码变更**（`5dc8fba` P0-10+P1-7 / `793c6dc` U-1 / `f1d1724` U-9 / `852a256` v1.4.202 / `012343d` 欢迎页）。
**P0-10（编辑/删除假成功）+ P1-7（实验室课程详情入口）已修复并经代码复核确认**；作者另外补齐了 `@system.app` feature 声明（未登记编号，但确实修好了「主页按返回键失灵」）。
10 条用户操作链路走查 **「不通」= 0 条**。9 条历史 P0 **逐条复核零回归**。
**新增未修：P1-9（3 页缺 rect/capsule 适配）、P1-10（输入法 `adjustScreenWidth` 回调 `this` 丢失，本轮 ES5 改造引入的回归）**。

| | P0 | P1 | 合计 |
|---|---|---|---|
| 🟢 已验证修复 | **10** | 8 | **18** |
| 🟡 部分改善 | 0 | 8 | 8 |
| 🔴 未修 | **0** | **4**（P1-9、P1-10 新增；P1-8 挂账；U-1 真机待验） | **4** |
| ⏸ 挂起（等作者答） | 0 | 2（P1-4 排队中、U-1 真机已问 6 轮） | 2 |
| **合计** | **10** | **22** | **32** |

> **2026-09-12 第 9 轮新增**：**P1-9**（`week-grid-demo.ux:247` / `week-grid-simple.ux:183` / `week-text-simple.ux:251` 只有 `@media (shape: circle)`，缺 rect / capsule —— 9 Pro / 10 Pro 方形屏布局风险，入口在实验室）、**P1-10**（`InputMethod.ux:808-817` 回调改成 `function` 后 `this` 丢失 → `screenWidth` 恒为 336 → 非 336 宽机型键盘偏移）、**P2-11**（`welcome.ux:120/133` 去掉防重入锁，双击重复 replace）、**P2-12**（`detail.ux:353-355` 参数缺失仍 `selectCourse(0)`，残留体验问题，已不丢数据）。
> ⚠️ 关键前提：手环端**联不了网**（Vela 官方 `system.fetch` / `system.request` 在手环全系 Not supported），所以 P1-8 **不要按「加个 fetch 上报」来做**，见该条目内的推荐方案。

## ⚠️ 作者登记 vs QA 复核 · 差异清单（本轮重点）

作者登记与代码实际状态**有 7 处对不上**。按规则「文档会过期，只信代码」，一律以代码复核为准：

### A. 作者标「待办/处理中」，实际 **cb94d64 已经修好了**（别重复劳动）

| 编号 | 作者登记 | 代码实际（QA 复核） | 结论 |
|---|---|---|---|
| **P0-2** | ② 处理中（下次提交修缓存） | `add-course.ux:505` 已走 `database.insertCourse`；**直写 `storage.setItem` 0 处、`allCourses_` 0 处**；且 `database.insertCourse` 内部已有 `if (!err) invalidateCache(currentScheduleIndex)` —— 缓存问题一并解决 | ✅ 已修 |
| **P0-3** | ② 处理中（下次提交改 `notes: ""`） | `detail.ux:379` `self.courseNotes = c.notes \|\| ""` 读取原值，`:559` `notes: this.courseNotes \|\| ""` 回写 —— 硬编码 `notes: ""` **已不存在** | ✅ 已修 |
| **P1-1** | ③ 打算做（补 @media 适配） | `@media (shape:...)` 覆盖 **29/29 页面，0 页缺失** | ✅ 已修 |
| **P1-5** | ③ 打算做（加错误提示 UI） | `index-full.ux:52` 已有 `<text class="empty-text">数据加载失败</text>` + 「重试」×1 + `error`×6 | ✅ 已修 |
| **U-12** | ③ 打算做（去重合并） | `pages/countdown-demo/` 目录**已整个删除** | ✅ 已修 |

### B. 作者标「不打算做」，但理由不成立 —— **我不关闭，继续挂起**

| 编号 | 作者填的理由 | 为什么不能关闭 |
|---|---|---|
| **U-1** 输入法 | 「需要真机联调才能确认具体现象，目前无法静态修复」 | 同一份登记表里「真机现象回填」写的是「**还没上真机，待测试后回填**」→ 这是**还没测**，不是**决定不修**。若按「不打算做」标 ⚪，这条会从台账永久消失，而你明确说了要测。→ 维持 **⏸ 挂起（已问 3 轮）**，等你上真机回填 A/B/C |
| **U-3** 字体设置 | 「目前仅在首页生效是临时设计，后续考虑全 App 铺开」 | 这实际是「**当前有意、后续要做**」，属于 ③ 打算做 → 改判 **🔴 未修（低优先）**，备注你的答复。**已解除卡住状态**，不再每轮追问 |

### C. 作者计划与代码现状不一致（目标未达成，但危害已消除）

| 编号 | 作者计划 | 代码现状 |
|---|---|---|
| **P1-6** | 详情页参数改为**路由参数**传递 | 仍走 `storage` 传 `detail_classId`/`detail_day`，**没改成路由参数**；但已加 `fail` 回调 → 失败 `showToast("请重试")` 且**不再 push**。「编辑到上一门课」的危害已消除。→ 保持 🟢，路由参数改造留作可选优化 |

---

## 状态图例（与作者 `REPLY.md` 三区登记一一对应）

| 标记 | 含义 | 作者那边填的是 | 谁来推进 |
|---|---|---|---|
| 🔴 未修 | 代码复核确认仍存在 | ③ 待处理·打算做（或没填） | 作者 |
| 🟠 处理中 | 作者说正在改，还没提交 | ② 处理中 | 作者（我不催） |
| 🟡 部分改善 | 指标好转但未达标 | ① 已处理（但只做了一部分） | 作者 |
| 🟢 已验证修复 | 我打开代码确认已修 | ① 已处理 | — |
| ⏸ 挂起 | 需要你回答才能定级（已问 N 轮） | 「真机现象」没填 | 作者 |
| ⚪ 已关闭 | 有意为之 / 不是问题 | ③ 待处理·不打算做 | — |

---

## P0 · 用户用不了

| 编号 | 标题 | 状态 | 复核证据（ef095da 第 7 轮） | 作者登记 | 修复 commit |
|---|---|---|---|---|---|
| **P0-10** | 编辑/删除课程「假成功」：id 未命中时静默不写库，仍提示已保存，改动 100% 丢失 | 🔴 **未修（第 2 轮）** | **`e12a13c` 复核：仍无 `hit` 标记** —— `database.js:291-309`（update）、`:332-347`（delete）未命中照旧 `callback(undefined)`；`detail.ux:354` 的 `!classId → selectCourse(0)` 兜底**一行未动**（`selectCourse` `:420` 仍从不设 classId）；`detail.ux:553/585` 仍判 `!err` → toast「课程已更新/已删除」+ back。入口 = 实验室「课程详情」（P1-7） | 未登记 | — |

**以下 9 条为 `cb94d64` 修复项，第 7 轮（`ef095da`）逐条复核：零回归 ✅**

| 编号 | 标题 | 状态 | 复核证据（cb94d64） | 作者登记 | 修复 commit |
|---|---|---|---|---|---|
| P0-1 | 删除课表导致课程数据串表 | 🟢 已验证 | `deleteScheduleAndShift` **0 处 → 8 处调用** | ① 已处理 ✅一致 | `cb94d64` |
| P0-2 | 新增课程保存后首页不显示 | 🟢 已验证 | `add-course.ux:505` 走 `database.insertCourse`；直写 storage / `allCourses_` **全部消失**；`insertCourse` 内部自带 `invalidateCache` | ⚠️ 作者标②处理中 → **实际已修** | `cb94d64` |
| P0-3 | 编辑课程静默清空备注 | 🟢 已验证 | `detail.ux:379` 读 `c.notes`，`:559` 回写 `this.courseNotes`，硬编码 `""` 已消失 | ⚠️ 作者标②处理中 → **实际已修** | `cb94d64` |
| P0-4 | `day` 异步竞态，课程存错天 | 🟢 已验证 | `add-course.ux:490` 保存前 `setDefaultDay()` 兜底；`setDefaultDay`(`:294`) 改纯同步 | ① 已处理 ✅一致 | `cb94d64` |
| P0-5 | 备份遗漏主题 | 🟢 已验证 | `backup-restore.ux:76` 已改为 `"appTheme"` | ① 已处理 ✅一致 | `cb94d64` |
| P0-6 | 复制课表失败卡在中间态 | 🟢 已验证 | 6 处 fail 分支 + 8 处 showToast；`inserted === total` → `processed >= total` | ① 已处理 ✅一致 | `cb94d64` |
| P0-7 | 激活持久化失败无反馈 | 🟢 已验证 | `activation.ux` 11 处 showToast + 4 处 else | ① 已处理 ✅一致 | `cb94d64` |
| P0-8 | 首页按返回退不出应用 | 🟢 已验证 | 全仓 `onBackPress` **0 处 → 1 处** | ① 已处理 ✅一致 | `cb94d64` |
| P0-9 | 「模块测试」跳未注册路由跳砖 | 🟢 已验证 | `lab-module-test` 引用**归零** | ① 已处理 ✅一致 | `cb94d64` |

> 作者登记的 7 条 ① 已处理，与我逐条复核结果**完全一致**。差异只在他低估了自己 —— P0-2/P0-3 其实也已经修好了。

## P1 · 界面与显示

### 机型适配（round3）

| 编号 | 标题 | 状态 | 复核证据（第 7 轮） | 作者登记 | 修复 commit |
|---|---|---|---|---|---|
| **P1-7** | 实验室「课程详情」入口不传 `detail_classId`，打开上一门课/空白兜底（P0-10 主入口） | 🔴 **未修（第 2 轮）** | **`e12a13c` 复核**：`lab-list.js:26` 仍含 `{ name: "课程详情", uri: "/pages/detail" }`；`lab.ux:101` 仍直接 `router.push(item.uri)` 不写 storage。全仓 3 个 detail 入口中 `class-list.js:131`、`week-view.ux:666` 已写 storage，**唯 lab 漏网** | 未登记 | — |
| P1-1 | 4 个页面零屏幕形状适配 | 🟢 已验证 | `@media (shape:...)` 覆盖 **29/29**（原 25/29） | ⚠️ 作者标③打算做 → **实际已修** | `cb94d64` |
| P1-6 | 详情页参数靠全局 storage，失败会编辑到「上一门课」 | 🟢 已验证 | `class-list.js` 改用 success/fail 回调，失败 `showToast` 且**不再 push**；**注：仍走 storage，未改成路由参数** | ③ 打算做（目标=路由参数，未做） | `cb94d64` |
| P1-5 | 数据库失败无错误态，永久「加载中」 | 🟢 已验证 | `index-full.ux:52`「数据加载失败」+ 重试 ×1 + error ×6 | ⚠️ 作者标③打算做 → **实际已修** | `cb94d64` |
| P1-2 | 顶部安全区不一致 | 🟡 部分改善 | `index-full.ux` 硬编码 `padding-top: 8px` 已移除；真机需确认观感 | ③ 打算做 ✅一致 | `cb94d64` |
| P1-3 | 圆屏下四角控件被裁切 | 🟡 部分改善 | shape 适配已全覆盖，四角裁切大概率顺带解决，**真机确认** | ③ 打算做 ✅一致 | `cb94d64` |
| P1-4 | 全站写死 px，无分辨率折算 | 🔴 未修 | `manifest.json` 仍 `designWidth: "device-width"`（**注：这是快应用标准做法，可能本就不算缺陷，待你确认后决定关闭还是保留**） | ③ 打算做 ✅一致 | — |

### UI 专项（round4）

| 编号 | 标题 | 状态 | 复核证据（基线 → 现在） | 作者登记 | 修复 commit |
|---|---|---|---|---|---|
| U-12 | 实验室「输入键盘」与「中文输入」重复 | 🟢 已验证 | `countdown-demo` 目录**已整个删除**（`e12a13c` 复核仍 0 处） | ⚠️ 作者标③打算做 → **实际已修** | `cb94d64` |
| **U-2** | T9 键盘缺 `jp.png` | 🟢 **已验证（第 8 轮）** | `src/components/InputMethod/assets/t9/jp.png` **已存在**，与 full/horizontal/arc 四套齐平 | ① 已处理 ✅一致 | `cb68d33` 系列 |
| **U-8** | 返回/取消按钮文案不统一 | 🟢 **已验证（第 8 轮）** | 英文 `back` **4 处 → 0 处**；符号统计仅剩 `◀`×28 | ① 已处理 ✅一致 | `cb68d33` 系列 |
| **P1-2** | 顶部安全区不一致 | 🟢 **已验证（第 8 轮）** | `padding: 44px` 覆盖 **29/29 页面** | ① 已处理 ✅一致 | `cb68d33` 系列 |
| **P1-3** | 圆屏下四角控件被裁切 | 🟡 已改·待真机 | `e12a13c` 为 6 个 overlay/modal 页（lab / reset-data / homepage-settings / backup-restore / schedule-manager / settings）补 `@media (shape: circle)` 下 `padding: 44px 36px` | ① 已处理 ✅一致 | `e12a13c` |
| U-11 | 双轨配色：`<style>` 写死深色 vs 模板 `{{theme.*}}` | 🟡 部分改善 | 硬编码色：`activation` 13→**12**、`backup-restore` 1→**0**、`reset-data` 1→**0**；但 `detail`/`add-course`/`settings`/`statistics` 等仍各 **13 种**，`week-overview-demo` 仍 **49 种**（最差） | ① 已处理 ✅一致 | `34f71bb` |
| U-4 | 8–11px 微小字号 | 🟡 部分改善 | <13px **76 → 56 → 50 处**（仅剩 12px 一档） | ① 已处理 ✅一致 | `cb68d33` 系列 |
| U-5 | 行高覆盖率低 | 🟡 部分改善 | **5%（34/678）→ 12.6%（119/942）→ 22.3%（210/942）**；作者称「29 页全部完成」，实测 6 页仍仅 1 处（activation/settings/schedule-manager/vibration-lab/homepage-settings/week-overview-demo） | ① 已处理，⚠️ 与代码有出入 | `7890156` `cb68d33` |
| U-6 | 长文本无溢出保护 | 🟡 部分改善 | 有溢出保护页面 **4 → 10 → 13 / 29** | ① 已处理 ✅一致 | `cb68d33` 系列 |
| U-7 | 方屏按钮热区过小 | 🟡 部分改善 | 目标机型（rect/circle）返回键 **28 → 40px** ✅；但全站 <40px 元素 **180 → 184**（+4 为「未判定」补显式高度所致的计量口径变化，welcome 实际按钮 52/44px 达标）；capsule 与未匹配 shape 仍回落 34px 基线 | ① 已处理 ✅一致 | `cb68d33` 系列 |
| U-9 | 彩色 emoji 当图标 | 🟡 部分改善 | 彩色 emoji **77 → 60 处**（本轮清 17 处）；**主题色板 `store.js:68-236` 9 个仍全是 emoji**（🔵🟢🔴⚫⬛🟣⬜🟡🌲🟠）+ 7 个页面的 `icon: '🔵'` | ① 已处理，⚠️ 主题色板未清 | `87c25a4` |
| U-10 | 页面加载/空/错误三态缺失 | 🟡 轻微改善 | 完全无三态页面 **8 / 29（与第 7 轮持平）**；本轮仅 `course-manager` 补了空态 | ① 已处理，⚠️ 整体未改善 | `cb68d33` 系列 |
| U-3 | 「字体大小」设置只在首页生效 | 🔴 未修（低优先） | 动态字号页面 **4/30 → 5/29**；作者答复「**临时设计，后续考虑全 App 铺开**」→ 当前为有意为之，**已解除卡住** | ③ 不打算做 → **改判 🔴**（未修但不再追问） | — |
| U-1 | 输入法 78 处绝对定位硬像素 | ⏸ 挂起（**已问 5 轮**） | **`e12a13c` 复核：一个数没变** —— 78 处绝对定位、`left` 最大 406px、**箭头函数 3 处**（`InputMethod.ux:810/813`、`dicUtil.js:59`）；作者答复「还没上真机，待测试后回填」 | ③ 不打算做 → **不关闭**，维持 ⏸ | — |

### 可观测性（2026-09-12 新增 · 用户提问驱动）

| 编号 | 标题 | 状态 | 复核证据（基线） | 作者登记 | 修复 commit |
|---|---|---|---|---|---|
| **P1-8** | 崩溃无留痕：无错误捕获、无本地日志、无回传通道 —— 用户现场对作者完全黑盒 | 🔴 **未修** | **`e12a13c` 复核：指标纹丝不动** —— 全局 `onError`/`onUnhandledRejection` **0 处**；空或纯日志 `catch(e){}` **14 处**；全仓 `fetch` **0 处**；`schemaVersion`/`migrate` 0 处。用户白屏/按钮失效时作者无从得知，只能等投诉（通常 <5%） | 未登记 | — |

> ⚠️ **先说结论：手环端联网这条路是死的，别走。**
> Vela 官方 `system.fetch`（数据请求）与 `system.request`（下载）支持明细里 —— **手环 8 Pro / 9 / 9 Pro / 10 / 10 Pro、Redmi Watch 4 全部 Not supported**；只有 Watch S1 Pro / S3 / S4 / S5、Redmi Watch 5 / 6 支持。
> 民间实测（米坛 #24134）：9 Pro 其实能跑 `fetch`，但**仅支持 http、不支持 https**；10 Pro 需额外装 AstroBox「网桥 FetchBridge」插件。
> 代价远大于收益。**结论：不要做崩溃自动上报。**

**✅ 推荐方案：本地错误日志 + 二维码扫码上报（全程不需要网络）**

仓库里**已有现成轮子，不必自造**：

| 已有资产 | 位置 | 说明 |
|---|---|---|
| 原生 `<qrcode>` 组件 | `schedule-qrcode.ux:29`、`activation.ux:435`、`donate.ux:119`、`qrcode-generator.ux` | `<qrcode value="{{ qrText }}" style="width:200px;height:200px">` |
| 载荷能力已验证 | `schedule-qrcode.ux:140` | 已把**整张课表文本**塞进二维码 → 容量与「手环屏能否扫」**已实证** |
| 纯 JS 矩阵生成器（备用） | `src/lib/qrcode.js`（561 行，ES5） | `module.exports = QRCode`，不需要原生组件时兜底 |

**三步落地**

1. `app.ux` 加全局 `onError` → 错误写入 storage **环形队列**（保留最近 20 条：时间 / 页面 / 消息 / 栈首行）
2. 新增「诊断」页 → 压成紧凑串 → `<qrcode>` 渲染；错误条数多时**分屏**（1/3、2/3、3/3 各一个码）
3. 服务端新增 `api/diag.js`（GET）→ 解析 query → 写 Redis `diag:log`（`LPUSH` + `LTRIM 0 499`）；`admin_Dx23.html` 加一个「崩溃日志」tab

**实现硬约束（不写清楚必踩坑）**

1. **载荷 ≤ 150 字符/屏** —— URL 前缀本身就占字节。用固定字典压成 `版本|页面编号|错误类型码|时间戳|次数`，**不要塞完整堆栈**
2. **尺寸给到 260–300px** —— 9 Pro / 10 Pro 屏宽 336px；现有 `schedule-qrcode` 的 200px 是给课表用的，诊断码字符更密，沿用会太挤
3. **白底 + 静默区** —— 沿用现有 `qr-wrapper` 的白底 padding，二维码别贴边
4. **必须 ES5** —— 新代码里出现 `?.` / `??` / 箭头函数，老内核会**整段不执行** → 诊断页自己变成白屏（最讽刺的失败方式）
5. **纯文本兜底** —— 万一某机型 `<qrcode>` 渲染失败，页面仍需显示错误码文本，供用户念/打进 QQ 群 936886288

**预期收益**：把「用户遇到白屏 → 你永远不知道」变成「用户遇到白屏 → 扫码 3 秒 → 你收到原始错误」。覆盖率仍依赖用户主动扫码，但恰好匹配「愿意反馈的用户」这一个最有价值的群体。

## P2 · 仅追踪（不进待办，不催修）

| 编号 | 标题 | 状态 | 说明 |
|---|---|---|---|
| U-13 | `<text onclick>` 当按钮无按压态 | 🔴 未修 | 全仓，约 35 处 |
| U-14 | ≤120px 窄容器 | 🔴 未修 | 全仓，约 254 个 |
| U-15 | `fontScale` 死配置，无入口 | 🔴 未修 | `store.js` |
| **P2-1** | 诊断上报接口 `api/diag.js` 为 GET 且无鉴权 → 任何人可伪造/刷假崩溃日志 | ⏸ **待实现后再判定** | 随 P1-8 引入新端点时才会产生。属「只影响日志可信度、不影响用户使用与资金」，按既定口径：**不急修、不进待办、不排期、不推飞书** |

---

## ⏸ 还需要你回答的：2 件事

1. **U-1 输入法**（已问 **5 轮**）—— 真机上是 A 完全唤不起 / B 偏右被切 / C 能出但打不出字？
   代码侧 78 处绝对定位 + 3 处箭头函数**完全没动**。你说了要上真机测，所以这条**没有关闭**，等你回填一个字母。
   如果真机完全正常，请明确回一句「真机没问题」→ 我立刻标 ⚪ 永久关闭。
2. **P1-4** `designWidth: "device-width"`（第 7 轮新增，第 8 轮再问）—— 这是快应用官方推荐写法，
   我倾向**这不算缺陷**。你说一句「不算」我就标 ⚪ 永久关闭。

> ~~U-3 字体设置范围~~ → 已答复（临时设计，后续考虑），**解除卡住**，改 🔴 低优先。
> ~~P0-8 返回键~~ → 代码已加 `onBackPress`，**问题自动消解**。

## 🎯 建议下一步（按性价比排序 · 第 8 轮）

1. **P0-10 编辑假成功**（约 3 行）：`updateCourseStorage` / `deleteCourseStorage` 加命中标记 `hit`，未命中 `callback(formatError(...))`。这是目前**唯一会造成用户数据静默丢失**的问题，优先级高于一切。
2. **P1-7 删掉实验室「课程详情」入口**（`lab-list.js:26` 一行删除）—— 顺手把 P0-10 的主入口堵上。
3. **先上真机验三条**（10 分钟）：新增课程 → 首页立即可见（不用重启）／编辑课程 → 备注还在／删课表 → 不串表
4. **U-1 的 3 处箭头函数改掉**（`InputMethod.ux:810/813`、`dicUtil.js:59`）—— 零风险，改完输入法至少不会因为语法问题整段不执行
5. **U-9 主题色板 9 个 emoji** → Unicode 符号或纯色块（`store.js:68-236`）
6. **P1-8 崩溃留痕**（非阻塞，等上面 1–5 做完再排）—— 复用现成 `<qrcode>` 组件，三步：本地环形日志 → 诊断页出码 → 服务端收。**注意不要走联网上报那条路**，手环全系不支持 `fetch`。
7. ~~U-2 / U-8 / P1-2 / P1-3~~ → 本轮已修（P1-3 待真机确认），别再花时间
8. ~~9 条历史 P0~~ → 本轮复核零回归，只需防「改回去」

## 报告索引

| 轮次 | 日期 | 类型 | 报告 | HEAD |
|---|---|---|---|---|
| **round8** | **2026-09-12 06:16** | **功能复验 + 回归 + UI 复测** | `docs/qa-reports/class-schedule-QA-20260912-0616.md` | **`e12a13c`** |
| round7 | 2026-09-11 23:45 | 功能复验 + 回归 | `docs/qa-reports/class-schedule-QA-20260911-2345.md` | `ef095da` |
| 作者登记处理轮 | 2026-09-11 23:25 | 登记复核 | 本表「差异清单」 | `cb94d64` |
| 复核轮 | 2026-09-11 23:20 | 修复复核 | 本表「复核证据」列 | `cb94d64` |
| round4 | 2026-09-11 21:30 | UI 专项 | `docs/qa-reports/class-schedule-UI-QA-20260911-2130.md` | `26afaf0` |
| round3 | 2026-09-11 17:25 | 功能复验 | `docs/qa-reports/class-schedule-QA-20260911-1725.md` | `26afaf0` |
| round2 | 2026-09-11 06:57 | 功能 | `HANDOFF/2026-09-11-round2.md` | — |
| round1 | 2026-09-11 00:42 | 功能 | `HANDOFF/2026-09-11-round1.md` | — |

---

## 维护规则（QA 侧）

每轮质检结束后按顺序更新本表：

1. **扫 commit**：`git log <上轮HEAD>..HEAD --grep='fix(qa)'` 摘编号 → 标 🟡 已修待验
2. **读 `REPLY.md` 三区登记**，逐条搬运：
   - ① 已处理 → 🟡 → 复核后 🟢；② 处理中 → 🟠（不催）；③ 打算做 → 🔴；③ 不打算做 → ⚪
3. **⚠️ 作者登记会滞后**：他填「处理中 / 打算做」的条目，**必须逐条打开代码复核** —— 本轮就发现 5 条他以为没修、实际 `cb94d64` 已经修好（P0-2/P0-3/P1-1/P1-5/U-12）。差异写进「差异清单」A 区
4. **⚠️ 「不打算做」要验理由**：若理由写的是「待测试 / 后续考虑」，本质是**还没决定**，标 ⏸ 挂起而非 ⚪ 关闭（本轮 U-1、U-3 均属此类）
5. **UI 指标回归**：跑 `scripts/ui-audit-schedule.py`，与「复核证据」列的基线数字对比
6. 新增缺陷追加行；更新顶部总结数字、「最近更新」、HEAD
7. ⏸ 挂起的条目在「还需要你回答的事」里带上"已问 N 轮"
