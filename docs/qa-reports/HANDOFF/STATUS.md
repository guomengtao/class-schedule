# 缺陷总台账（唯一权威状态表）

> **看这一张表就够了。** 每一轮质检后由 QA 更新，作者**只读**。
> 作者那边对应的是 `REPLY.md` 的**三区登记**（① 已处理 / ② 处理中 / ③ 待处理）—— 两边用同一套编号，可以直接对着看。
> 初始化：2026-09-11（round3 功能 + round4 UI）｜最近更新：**2026-09-11 23:20 复核轮**｜HEAD **`cb94d64`**（版本 1.4.193）

## 🟢 本轮复核结论：P0 全部清零

作者于 22:56 推送 `cb94d64`（53 文件，+5338/-2799），**9 条 P0 全部修复并经代码复核确认**。
未填 `REPLY.md`、未用 `fix(qa):` 编号 —— 走的是「只改代码不说话」，由 QA 自行复核。

| | P0 | P1 | 合计 |
|---|---|---|---|
| 🟢 已验证修复 | **9** | 4 | **13** |
| 🟡 部分改善 | 0 | 7 | 7 |
| 🔴 未修 | 0 | 5 | 5 |
| ⏸ 挂起（等作者答） | 0 | 2 | 2 |
| **合计** | **9** | **18** | **27** |

## 状态图例（与作者 `REPLY.md` 三区登记一一对应）

| 标记 | 含义 | 作者那边填的是 | 谁来推进 |
|---|---|---|---|
| 🔴 未修 | 代码复核确认仍存在 | ③ 待处理·打算做（或没填） | 作者 |
| 🟠 处理中 | 作者说正在改，还没提交 | ② 处理中 | 作者（我不催） |
| 🟡 部分改善 | 指标好转但未达标 | ① 已处理（但只做了一部分） | 作者 |
| 🟢 已验证修复 | 我打开代码确认已修 | ① 已处理 | — |
| ⏸ 挂起 | 需要你回答才能定级（已问 N 轮） | 「真机现象」/「不打算做」没填 | 作者 |
| ⚪ 已关闭 | 有意为之 / 不是问题 | ③ 待处理·不打算做 | — |

---

## P0 · 用户用不了 —— 🎉 全部已修

| 编号 | 标题 | 状态 | 复核证据（cb94d64） | 修复 commit |
|---|---|---|---|---|
| P0-1 | 删除课表导致课程数据串表 | 🟢 已验证 | `deleteScheduleAndShift` **0 处 → 8 处调用** | `cb94d64` |
| P0-2 | 新增课程保存后首页不显示 | 🟢 已验证 | `add-course.ux:509` 改走 `database.insertCourse`；直写 storage 与 `allCourses_` **全部消失** | `cb94d64` |
| P0-3 | 编辑课程静默清空备注 | 🟢 已验证 | `detail.ux` 中 `notes: ""` 硬编码**已消失** | `cb94d64` |
| P0-4 | `day` 异步竞态，课程存错天 | 🟢 已验证 | `add-course.ux:490` 保存前 `setDefaultDay()` 兜底；`setDefaultDay`(`:294`) 改为纯同步 `new Date()`，不再依赖 storage 回调 | `cb94d64` |
| P0-5 | 备份遗漏主题 | 🟢 已验证 | `backup-restore.ux:76` 已改为 `"appTheme"` | `cb94d64` |
| P0-6 | 复制课表失败卡在中间态 | 🟢 已验证 | `schedule-manager.ux` 出现 **6 处 fail 分支 + 8 处 showToast** | `cb94d64` |
| P0-7 | 激活持久化失败无反馈 | 🟢 已验证 | `activation.ux` 出现 **11 处 showToast + 4 处 else** | `cb94d64` |
| P0-8 | 首页按返回退不出应用 | 🟢 已验证 | 全仓 `onBackPress` **0 处 → 1 处** | `cb94d64` |
| P0-9 | 「模块测试」跳未注册路由跳砖 | 🟢 已验证 | `lab-module-test` 引用**归零** | `cb94d64` |

> P0-8 原本挂着"等你确认是否有意为之"，现在代码里已加 `onBackPress`，**问题自动消解，不用再回答了**。

## P1 · 界面与显示

### 机型适配（round3）

| 编号 | 标题 | 状态 | 复核证据 | 修复 commit |
|---|---|---|---|---|
| P1-1 | 4 个页面零屏幕形状适配 | 🟢 已验证 | `@media (shape:...)` 覆盖 **29/29 页面**（原 25/29） | `cb94d64` |
| P1-6 | 详情页参数靠全局 storage，失败会编辑到「上一门课」 | 🟢 已验证 | `class-list.js` 改用 `storage.set` 的 **success/fail 回调**，失败 `showToast("请重试")` 且**不再 push** | `cb94d64` |
| P1-5 | 数据库失败无错误态，永久「加载中」 | 🟢 已验证 | `index-full.ux` 现含 `error`×6、「失败」×1、「重试」×1 | `cb94d64` |
| P1-2 | 顶部安全区不一致 | 🟡 部分改善 | `index-full.ux` 硬编码 `padding-top: 8px` **已移除**，改用统一方案；真机需确认观感 | `cb94d64` |
| P1-3 | 圆屏下四角控件被裁切 | 🟡 部分改善 | shape 适配已全覆盖（见 P1-1），四角裁切大概率顺带解决，**真机确认** | `cb94d64` |
| P1-4 | 全站写死 px，无分辨率折算 | 🔴 未修 | `manifest.json` 仍 `designWidth: "device-width"`（**注：这是快应用标准做法，可能本就不算缺陷，待你确认后决定关闭还是保留**） | — |

### UI 专项（round4）

| 编号 | 标题 | 状态 | 复核证据（基线 → 现在） | 修复 commit |
|---|---|---|---|---|
| U-12 | 实验室「输入键盘」与「中文输入」重复 | 🟢 已验证 | `countdown-demo` 目录**已整个删除** | `cb94d64` |
| U-11 | 双轨配色：`<style>` 写死深色 vs 模板 `{{theme.*}}` | 🟡 部分改善 | 硬编码色 `detail` 36→**13** 种、`add-course` 33→**13** 种 | `cb94d64` |
| U-8 | 返回/取消按钮文案不统一 | 🟡 部分改善 | **7 种 → 5 种**（`◀`×23、`back`×4、`◀ 返回`×1、`返回`×1、`取消`×1）；**仍有 4 处英文 `back`** | `cb94d64` |
| U-4 | 8–11px 微小字号 | 🟡 部分改善 | <13px **76 处 → 56 处** | `cb94d64` |
| U-5 | 行高覆盖率低 | 🟡 部分改善 | **5%（34/678）→ 12%（115/942）** | `cb94d64` |
| U-6 | 长文本无溢出保护 | 🟡 部分改善 | 有溢出保护页面 **4 → 7** | `cb94d64` |
| U-7 | 方屏按钮热区过小 | 🔴 未修 | <40px 按钮 **154 → 158 个**（微增，未改善） | — |
| U-9 | 彩色 emoji 当图标 | 🔴 未修 | `store.js` 主题 icon **仍全是 emoji**（🔵🟢🔴⚫⬛🟣⬜🟡🌲🟠🔵）；模板 emoji 104→84 处 | — |
| U-10 | 页面加载/空/错误三态缺失 | 🔴 未修 | 无任何三态的页面 **9 → 12 个**（新增页面也缺） | — |
| U-2 | T9 键盘缺 `jp.png` | 🔴 未修 | `assets/t9/` **仍无 jp.png**，切日文继续裂图 | — |
| U-1 | 输入法 78 处绝对定位硬像素 | ⏸ 挂起（已问 2 轮） | **78 处、`left` 最大 406px、箭头函数 2 处，一个数没变** —— 需要你真机确认现象 | — |
| U-3 | 「字体大小」设置只在首页生效 | ⏸ 挂起（已问 2 轮） | 动态字号页面 **4/30 → 5/29**（新增 `week-view`），仍未铺开 —— 需要你决定是否全 App 铺开 | — |

## P2 · 仅追踪（不进待办，不催修）

| 编号 | 标题 | 状态 | 说明 |
|---|---|---|---|
| U-13 | `<text onclick>` 当按钮无按压态 | 🔴 未修 | 全仓，约 35 处 |
| U-14 | ≤120px 窄容器 | 🔴 未修 | 全仓，约 254 个 |
| U-15 | `fontScale` 死配置，无入口 | 🔴 未修 | `store.js` |

---

## ⏸ 还需要你回答的 2 件事（其余都已解决）

1. **U-1 输入法** —— 真机是 A 完全唤不起 / B 偏右被切 / C 能出但打不出字？
   （代码侧 78 处绝对定位完全没动，如果你真机没问题，这条就降级）
2. **U-3「字体大小」设置** —— 全 App 铺开，还是保持只有首页生效？

> 原第 3 件事（P0-8 返回键）已由代码自动解决，**不用再答**。

## 报告索引

| 轮次 | 日期 | 类型 | 报告 | HEAD |
|---|---|---|---|---|
| 复核轮 | 2026-09-11 23:20 | 修复复核 | 本表「复核证据」列 | `cb94d64` |
| round4 | 2026-09-11 21:30 | UI 专项 | `docs/qa-reports/class-schedule-UI-QA-20260911-2130.md` | `26afaf0` |
| round3 | 2026-09-11 17:25 | 功能复验 | `docs/qa-reports/class-schedule-QA-20260911-1725.md` | `26afaf0` |
| round2 | 2026-09-11 06:57 | 功能 | `HANDOFF/2026-09-11-round2.md` | — |
| round1 | 2026-09-11 00:42 | 功能 | `HANDOFF/2026-09-11-round1.md` | — |

---

## 维护规则（QA 侧）

每轮质检结束后按顺序更新本表：

1. **扫 commit**：`git log <上轮HEAD>..HEAD --grep='fix(qa)'` 摘编号 → 标 🟡 已修待验
2. **读 `REPLY.md` 三区登记**，逐条搬运：
   - ① 已处理 → 🟡 → 复核后 🟢；② 处理中 → 🟠（不催）；③ 打算做 → 🔴；③ 不打算做 → ⚪
3. **打开代码复核**（文档会过期，只信代码）→ 真修好转 🟢，只做一半标 🟡，没动退回 🔴
4. **UI 指标回归**：跑 `scripts/ui-audit-schedule.py`，与「复核证据」列的基线数字对比
5. 新增缺陷追加行；更新顶部总结数字、「最近更新」、HEAD
6. ⏸ 挂起的条目在「还需要你回答的事」里带上"已问 N 轮"
