# 第 11 轮 QA 交接本 · 2026-09-13

- **本轮 HEAD**：`d9b9bf6`（v1.4.209）
- **上一轮基线**：`7d5f014`（round10 黑屏根因裁定）→ **本轮有真实代码变更**（4 个提交，含删 4 页 + 输入法重构）
- **完整报告**：[`class-schedule-QA-20260913-0927.md`](../class-schedule-QA-20260913-0927.md)
- **走查结果**：通 8 · 部分不通 1 · 不适用 1 · 不通 0

---

## 本轮待办（只放 P0 / P1）

| 顺序 | 编号 | 标题 | 优先级 | 状态 | 定位 | 建议顺序理由 |
|---|---|---|---|---|---|---|
| 1 | **P1-10** | 胶囊屏键盘右移 72px（屏宽 37.5%）—— 本轮重构后影响面扩到**全部**文本输入 | P1（**建议按 P0 排期**） | 🔴 未修（第 3 轮） | `InputMethod.ux:808-817`（`this` 丢失）+ `:407`（默认 336）+ 偏移量用于 `:265/:266/:303/:323` | 一行 `var self = this` 能解决一半；但还要加 `$watch("screentype")` 才真的会被调用。改动小、收益最大 |
| 2 | **P0-11** | 二维码生成器输入超过 5 字后按键完全无反应、无提示 | P0（新增） | 🔴 未修 | `qrcode-generator.ux:121-125` 漏写 `chinese_input_maxlen`；`chinese-input.ux:79` 默认 5；`:162` 静默 return | 1 行 storage.set 就能修；是唯一真 P0 |
| 3 | **P1-15** | 设置页胶囊屏描述不可见 —— **你新文档的归因错了，修复方案不会生效** | P1（新增） | 🔴 未修 | 真根因 `settings.ux:527 .row-label{flex-shrink:0}` + `:533 .row-right{flex-shrink:1;overflow:hidden}`；**不是** `:841 display:none`（死代码） | 先读报告第三节 P1-15，再决定怎么改，避免白改 |
| 4 | **P1-11** | 全仓 40 处 `@media (shape: capsule)` 无效，应为 `pill-shaped` | P1（根因级） | 🔴 未修（第 2 轮） | 全仓（本轮 27→40）。硬证据：`lab.ux:348-404` 你本轮为胶囊写的 33 行整段在无效块内 | **先用你自己新建的 `capsule-hide-test` 页判一次**（见下），再决定要不要全仓替换 |
| 5 | **P1-12** | 5 处基础层固定宽 > 胶囊 192px | P1 | 🔴 未修（第 2 轮） | `welcome.ux:211`(220px，首屏主按钮)、`welcome.ux:272`、`schedule-qrcode.ux:29`、`detail.ux:710`、`add-course.ux:613` | 改百分比，胶囊屏立刻受益 |
| 6 | **P1-13** | `.sheet-overlay` 四边定位，父容器塌缩时浮层 0×0 | P1 | 🔴 未修（第 2 轮） | `unlock-dialog.ux:97-106`、`bs-demo4.ux:180-188` | 改 `width/height:100%`，全仓仅此 2 处 |
| 7 | **P1-14** | `min-height:100%` 12 处（Vela 不支持） | P1 | 🔴 未修（8→12 处） | 本轮新增 `chinese-input.ux:204`、`nickname-edit.ux:156` | 改 `height:100%` |
| 8 | **P1-9** | `chinese-input-full.ux` 缺 `@media (shape: rect)` | P1 | 🔴 未修 | `chinese-input-full.ux`（circle 1 / capsule 1 / rect 0） | 10 行。注：上轮 3 个 week-* 页已随删页自然消失 |

---

## 需要你确认的两件事（不是缺陷，是判定分歧）

### ① P1-11 到底成不成立 —— 用你的 `capsule-hide-test` 一测即知

实验室 →「胶囊屏隐藏测试」→ 在米环 9/10 上打开：

- **6 个方法全部「✓ 可见」** → `@media (shape: capsule)` **没生效** → P1-11 成立，正确值是 `pill-shaped`；你本轮 33 行胶囊适配是死代码；设置页描述消失是 P1-15 的 flex 挤压。
- **6 个方法全部「✗ 隐藏」** → `capsule` 有效 → **我撤回 P1-11**，设置页就是 `:841 display:none` 造成的，你的方案方向对。

### ② U-1 输入法真机现象（**已问 8 轮**）

`InputMethod.ux` 的箭头函数早已改完，但「胶囊屏/方屏上键盘到底长什么样」一直没有真机回填。这次重构后所有输入都走这个组件，麻烦真机截一张图。

---

## 本轮确认「已修 / 别再报」

- `schedule-qrcode.ux` CSS 缺闭合括号（`d177fbb`）→ 全量括号平衡 **32/32** ✅
- 删 4 个 week-* 页后**无残留引用**（grep 0 命中），manifest 32 ↔ 目录 32，无跳砖 ✅
- 中文输入重构**未破坏调用契约**：6 个调用方与输入页的 `chinese_input_*` 键名逐项一致，`returnResult`/`trim`/`router.back` 与旧版逐行相同 ✅
- 语法层全绿：`node --check` 0 错误，ES6+ 高危语法（`?.`/`??`/async/模板串/箭头/解构/Promise）**全 0** ✅
- 历轮已修项零回归：`deleteScheduleAndShift` 8 / `onBackPress` 1 / `lab-module-test` 0 / `fetch(` 0 / `invalidateCache` 9 / `database.js` `hit` 标记在（`:291/:309`、`:339/:356`）/ `padding-top:44px` 32/32 ✅

---

## 作者回复区（Tom 填）

> 填在这里我下轮读 → 同步进 `STATUS.md` → 打开代码复核。也可以用 commit 代替：`git commit -m "fix(qa): P1-10 xxx"`。

| 编号 | 你的结论（①已处理给 commit / ②处理中 / ③打算做 / ③不打算做+理由） | commit |
|---|---|---|
| P0-11 | | |
| P1-10 | | |
| P1-15 | | |
| P1-11 | | |
| P1-12 | | |
| P1-13 | | |
| P1-14 | | |
| P1-9 | | |

**真机现象回填**

| 编号 | 真机现象 |
|---|---|
| `capsule-hide-test` 6 个方法的 ✓/✗ | |
| U-1 输入法（米环 9/10 键盘截图结论） | |
| P1-15 设置页描述在胶囊屏是否真的不可见 | |

**其他想对我说的**
