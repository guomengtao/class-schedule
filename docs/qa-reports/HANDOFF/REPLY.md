# 作者处理登记（唯一入口）

> **这个文件是作者(Tom)专用的。** QA 只读、永不新建同名文件、永不删除。
> **不管你在回哪一轮的问题，都写在这一个文件里。**
> 你填完 → 我下轮读 → 同步进 `STATUS.md` 的状态列 → 打开代码复核。**两边用同一套编号，可以直接对着看。**

---

## 你怎么标，我怎么记（对照表）

| 你在这里填 | 我在 `STATUS.md` 标 | 后续动作 |
|---|---|---|
| **① 已处理**（给了 commit） | 🟡 已修待验 → 复核后转 🟢 | 我下轮打开代码确认；没修实会退回 🔴 并说明 |
| **② 处理中** | 🟠 处理中 | 不催，下轮再看；连续 3 轮没动静我会提醒 |
| **③ 待处理 · 打算做** | 🔴 未修 | 保持排队，正常出现在待办表里 |
| **③ 待处理 · 不打算做 / 有意为之** | ⚪ 已关闭 | **不再报**，从此消失 |

> 也可以用 commit 代替填表：`git commit -m "fix(qa): P0-2 修了 xxx"` —— 我自动扫，一个字都不用写。
> 两种混用也行，以**最新**的为准。

---

## ① 已处理（改完了，填 commit）

| 编号 | 一句话说明 | commit |
|---|---|---|
| _例：P0-5_ | _例：备份键 theme → appTheme_ | _a1b2c3d_ |
| | | |
| | | |

## ② 处理中（正在改，还没提交）

| 编号 | 进展 / 卡在哪 | 预计 |
|---|---|---|
| _例：P0-1_ | _例：在改 deleteScheduleAndShift 的调用_ | _本周五_ |
| | | |

## ③ 待处理

### 打算做（排队中）

| 编号 | 计划 |
|---|---|
| _例：U-3_ | _例：字号统一放到下个大版本_ |
| | |

### 不打算做 / 有意为之（填了我就不再报）

| 编号 | 理由 |
|---|---|
| _例：P0-8_ | _例：首页按返回不退出是刻意的，防误触_ |
| | |

---

## 真机现象回填（我标了「无法静态确认」的，只有你能答）

| 编号 | 真机上到底是什么现象 |
|---|---|
| _例：U-1 输入法_ | _例：B，键盘偏右被切掉一截_ |

---

## 其他

_（随便写）_

---

## ⏸ 当前最需要你回的 3 件事（挂了 4 轮了）

1. **P0-8 首页按返回退不出应用** —— 有意为之，还是漏了 `onBackPress`？（填「不打算做」或「打算做」都行，别空着）
2. **U-1 输入法** —— 真机是 A 完全唤不起 / B 偏右被切 / C 能出但打不出字？（填上面「真机现象回填」）
3. **U-3「字体大小」设置只在首页生效** —— 全 App 铺开，还是保持现状？

---

## 全部编号速查（从 `STATUS.md` 同步，填表时直接复制）

**P0**：P0-1 删课表串表 ｜ P0-2 新增课程首页不显示(1行可修) ｜ P0-3 编辑清空备注 ｜ P0-4 day 竞态 ｜ P0-5 备份主题键(改1个字符串) ｜ P0-6 复制课表中间态 ｜ P0-7 激活无反馈 ｜ P0-8 返回退不出(待确认) ｜ P0-9 模块测试跳砖(删按钮即可)

**P1**：P1-1 四页零适配 ｜ P1-2 安全区不一致 ｜ P1-3 圆屏四角裁切 ｜ P1-4 全站写死px ｜ P1-5 无错误态 ｜ P1-6 详情页参数靠全局storage

**UI**：U-1 输入法绝对定位(待确认) ｜ U-2 缺 jp.png(补图) ｜ U-3 字号设置范围(待确认) ｜ U-4 微小字号 ｜ U-5 行高5% ｜ U-6 无溢出保护 ｜ U-7 方屏热区28×24 ｜ U-8 返回文案7种 ｜ U-9 emoji当图标 ｜ U-10 三态缺失 ｜ U-11 双轨配色 ｜ U-12 实验室重复
