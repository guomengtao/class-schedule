# QA 交接本 · class-schedule · 2026-09-11 round 1

> **QA 第 3 轮** / 交接本第 1 期（class-schedule 首次建立交接本）
>
> **沟通规则**：QA 每轮**只新建文件**，作者**只改已有文件的「作者回复区」** —— 双方写不同文件，从根本上避免 git 冲突。
> QA 下一轮会先读本文件的回复区，再**打开代码复核**是否真修（不会只信"已修"两个字）。

| 项 | 值 |
|---|---|
| 本轮 HEAD | `d4e11a6668cb5665dd25f68aeb40eaadf1ff82d7` |
| 上轮 HEAD | `2d727e98080454bef62717983fe25de94cde807d` |
| 代码变更 | **0**（`2d727e9..d4e11a6` 只多了 2 个 QA 文档） |
| 完整报告 | [`../class-schedule-QA-20260911-0035.md`](../class-schedule-QA-20260911-0035.md) |
| 版本 | versionName 1.4.153 / versionCode 653 |

---

## 一句话结论

零代码变更，前两轮缺陷全部原样遗留；本轮首次做完整操作链路走查，**新抓到 3 个"点了没反应 / 退不出来"的问题**，其中 **P0-8（首页按返回退不出应用）每个用户每天都会踩**。

---

## 本轮待办（只列 P0 / P0-S / P1）

| 编号 | 标题 | 优先级 | 状态 | 定位 | 建议顺序 |
|---|---|---|---|---|---|
| P0-8 | **首页按返回退不出应用，被启动页 3 秒倒计时循环吞掉** | P0 | 🔵 本轮新增·未修 | `welcome.ux:119`/`:126`（用 push 不是 replace）、`:52-57`（onShow 重启倒计时）、`:81-107`；全仓 `onBackPress` = 0 处 | **1（最先修）** |
| P0-2 | 新增/编辑课程保存后首页不显示，须重启（＝用户说的"编辑课程失败"） | P0 | 🔴 未修复 | `add-course.ux:510` 直写 storage，绕过 `database.insertCourse` | **2** |
| P0-1 | 删除课表导致课程数据串表（致命） | P0 | 🔴 未修复 | `schedule-manager.ux:389/414/427`；`data/database.js:374/576` 已有 `deleteScheduleAndShift` 但 0 调用 | **3** |
| P0-3 | 编辑课程静默清空备注（数据丢失） | P0 | 🔴 未修复 | `detail.ux:548`（`notes: ""` 硬编码，全文件无读取逻辑） | **4（改动最小，顺手就能修）** |
| P0-4 | `day` 异步竞态，课程存了但看不见 | P0 | 🔴 未修复 | `add-course.ux:161/231/235/293` | 5 |
| P0-5 | 备份遗漏主题（键写成 `"theme"`，实际是 `"appTheme"`） | P0 | 🔴 未修复 | `backup-restore.ux:72` vs `data/store.js:251/272/288` | 6（改一个字符串） |
| P0-9 | 「模块测试」跳未注册路由 `/pages/lab-module-test` → 跳砖 | P0 | 🔵 本轮新增·未修 | `vibration-lab.ux:525-530`；manifest 30 条路由中无此项 | 7（删按钮即可） |
| P0-6 | 复制课表失败卡在中间态 | P0 | 🔴 未修复 | `schedule-manager.ux:312/328/330` | 8 |
| P0-7 | 激活持久化失败无任何反馈 | P0 | 🔴 未修复 | `activation.ux:511-523` | 9 |
| P1-1 / P1-1b | 4 个页面零屏幕形状适配（index-full / welcome / schedule-manager / custom-content-edit） | P1 | 🔴 未修复 | 见报告 P1 表 | 10 |
| P1-2 | 顶部安全区不一致（首页 8px vs 全站 44px） | P1 | 🔴 未修复 | `index-full.ux:380` | 11 |
| P1-3 | 圆屏下四角控件被裁切不可点 | P1 | 🔴 未修复 | `index-full.ux:6/9/11-21/656/680` | 12 |
| P1-4 | 全站写死 px，无分辨率折算 | P1 | 🔴 未修复 | `manifest.json:57`（`designWidth: device-width`） | 13 |
| P1-5 | 数据库失败无错误态，永久「加载中」 | P1 | 🔴 未修复 | `index-full.ux:48-50` | 14 |

> 未列入：P2-1~P2-9（输入交互）、P3-2~P3-7（代码质量，含**输入法箭头函数待真机确认** P3-7）、P4-1（白嫖类，按你的定位不排期）。详见完整报告。

### 想请你重点确认的两件事

1. **「首页按返回到底应该怎样？」** 现在的实现是：按返回 → 回到启动页 → 3 秒后又自动跳回首页，等于退不出去。我按"应该能退出"来报的 P0-8，但如果这是**有意为之**（比如防止误触退出），告诉我一声我就降级。
2. **输入法到底打不开到什么程度？** 代码里 `chinese-input-full.ux:19` 把 `screentype` 写死成 `circle`，方屏的 9 Pro / 10 Pro 会按圆屏布局渲染（P2-2）。另外输入法链路是全仓**唯一**用了 ES6 箭头函数的地方（`InputMethod.ux:804`、`dicUtil.js:59`）。如果真机上是**完全唤不起来**，我倾向是箭头函数解析失败，麻烦真机点一下告诉我现象（完全没反应 / 出来了但错位 / 能出但打不出字）。

---

## 作者回复区（请直接在这里写，QA 下轮会读 + 复核代码）

### 已修条目

| 编号 | 修复说明 | 对应 commit |
|---|---|---|
| _（待填）_ | | |

### 认为不是问题 / 有意为之

| 编号 | 说明 |
|---|---|
| _（待填）_ | |

### 需要 QA 补充信息

| 编号 | 想了解什么 |
|---|---|
| _（待填）_ | |

### 其他想说的

_（待填）_
