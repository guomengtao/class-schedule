# QA 第 9 轮交接本 · 2026-09-12 12:44

- **本轮 HEAD**：`012343d`（版本 **1.4.202** / versionCode 702）
- **上一轮基线**：`c3ea2fe`（QA 文档）/ `e12a13c`（代码 1.4.198）
- **本轮报告**：[`../../../reports/class-schedule-QA-20260912-1244.md`](../../../reports/class-schedule-QA-20260912-1244.md)（仓库内：`docs/qa-reports/class-schedule-QA-20260912-1244.md`）
- **代码变更**：有 —— `5dc8fba`(P0-10+P1-7)、`793c6dc`(U-1)、`f1d1724`(U-9)、`852a256`(v1.4.202)、`012343d`(欢迎页进入按钮)

---

## 🎉 先说好消息

**P0-10（编辑/删除假成功，连续挂了 2 轮）和 P1-7（实验室课程详情入口）本轮已修复并经我代码复核确认。**
至此 **P0 全部清零**，10 条用户操作链路**「不通」= 0 条**。另外你上一轮补的 `@system.app` feature 声明（`852a256`）虽然没登记编号，但确实是修好了一个真问题：之前 `index-full.ux:358` require 了未声明的 feature，主页按返回键会失灵。

---

## 本轮待办

| 编号 | 标题 | 优先级 | 状态 | 定位 | 建议修复顺序 |
|---|---|---|---|---|---|
| **P1-9** | `week-grid-demo` / `week-grid-simple` / `week-text-simple` 三页只有 `@media (shape: circle)`，缺 rect / capsule 分支 → 9 Pro / 10 Pro 方形屏布局风险 | P1 | 🔴 未修 | `week-grid-demo.ux:247`、`week-grid-simple.ux:183`、`week-text-simple.ux:251` | **1**（照其它 26 页补两段 media query，成本极低） |
| **P1-10** | 本轮 ES5 改造引入回归：`adjustScreenWidth()` 回调里 `this` 丢失 → 真机屏宽取不到，`screenWidth` 恒为 336 → 非 336 宽机型键盘偏移 | P1 | 🔴 未修（本轮新增） | `src/components/InputMethod/InputMethod.ux:808-817`（`:810`、`:813`）；调用点 `:456-457`；默认值 `:407` | **2**（加 `var self = this`，一行） |
| **P2-11** | 欢迎页进入按钮去掉防重入锁，快速双击连发两次 `router.replace` | P2 | 🔴 未修（本轮新增） | `welcome.ux:120-131`、`:133-145` | 3（与 P2-12 一起改） |
| **P2-12** | 详情页参数缺失时静默 `selectCourse(0)` 打开「上一门课」（P0-10 修复后的残留体验问题，已不丢数据） | P2 | 🔴 未修 | `detail.ux:353-355`、`:420` | 3 |
| **P1-3** | 圆屏 modal 宽度 72% 的实际裁切效果 | P1 | ⏸ 待真机验证 | `e12a13c` | 真机顺手看一眼 |
| **P1-4** | 全站写死 px，`designWidth: device-width` | P1 | ⏸ 排队中（你 REPLY 已接受） | 全局 | 你排期 |
| **P1-8** | 崩溃无留痕 | P1 | ⏸ 长期挂账 | 全局 | 不阻塞 |
| **U-1 真机** | 输入法在手环上的真实现象 | P1 | ⏸ **已问 6 轮** | `chinese-input.ux` / `InputMethod.ux` | 真机顺手看一眼 |
| **P3-13** | `manifest.json` 末尾缺换行 | P3 | 🔴 未修 | `manifest.json:160` | 随手 |
| **P3-14** | `_hasEntered` 变成只写不读的死变量 | P3 | 🔴 未修 | `welcome.ux:56,126,129,140,143` | 随 P2-11 一起处理 |

---

## 作者回复区（Tom 填这里，我下轮来读）

### ① 本轮已处理

| 编号 | 处理结果 / commit |
|---|---|
|  |  |

### ② 打算做（排队中）

| 编号 | 计划 |
|---|---|
|  |  |

### ③ 不打算做 / 有意为之（填了我就不再报）

| 编号 | 理由 |
|---|---|
|  |  |

### 真机现象回填（只有你能答的）

| 编号 | 真机上到底是什么现象 |
|---|---|
| U-1 输入法 | （已问 6 轮：箭头函数已改完，A 套 `chinese-input.ux` / B 套 `InputMethod.ux` 在手环上分别什么表现？） |
| P1-9 三页 | （9 Pro / 10 Pro 上打开这三个周视图页，顶部有没有被裁？） |
| P1-10 键盘 | （10 Pro 上键盘是不是整体偏左？） |

### 其他想说的

| 内容 |
|---|
|  |

---

## 我本轮已经确认「这条是好的」，下轮不会重复报

- `database.js` 的 `hit` 标记（update `:291/:309-313`、delete `:339/:355-359`）✅
- `updateCourse` / `deleteCourse` 都带 `invalidateCache`（`:511`、`:521`）✅
- `add-course.ux:509` 走 `database.insertCourse`，不再直写 storage ✅
- 全仓箭头函数 **0** 处 ✅（`crypto.js:344-402` 的 `??` 是脱敏占位字符串，不是语法）
- 主题 `icon` 字段全仓**无任何引用**，改成 `●` 不影响显示 ✅
- 111 处静态跳转目标 100% 命中 manifest 29 页；`index-full.ux` 那 10 个「未定义」方法由 `modules/*.js` 注入，是审计误报 ✅
- 实验室 22 个入口 URI 全部有效 ✅
- 全仓 `fetch(` = 0 → 「数据同步」这条走查永远标注「不适用」✅
