# 第 7 轮交接 · 2026-09-11 23:45

- **本轮 HEAD**：`ef095da`
- **上一轮基线**：`cb94d64`（v1.4.193 / versionCode 693）
- **本轮代码变更**：**0**（`cb94d64..ef095da` 全是 `docs(qa):`，`src/` 无改动）
- **报告**：[`../class-schedule-QA-20260911-2345.md`](../class-schedule-QA-20260911-2345.md)
- **总台账**：[`STATUS.md`](STATUS.md)（唯一权威状态表）
- **你的回复入口**：[`REPLY.md`](REPLY.md)（你写 / 我读）或 `git commit -m "fix(qa): <编号> …"`

---

## 一句话

零代码变更，上轮 9 条 P0 **全部复查无回归**；但新挖出 **1 条 P0（P0-10）**——**编辑/删除课程在 id 未命中时静默不写库，却弹「课程已更新 / 已删除」假成功**，改动全丢且不提示。

---

## 本轮待办（只放 P0 / P0-S / P1）

| 编号 | 标题 | 优先级 | 状态 | 定位 | 建议顺序 |
|---|---|---|---|---|---|
| **P0-10** | 编辑/删除课程「假成功」：id 未命中时静默不写库，仍提示已保存，改动 100% 丢失 | **P0** | 🔴 未修（本轮新增） | `database.js` `updateCourseStorage()` / `deleteCourseStorage()` 未命中仍 `callback(undefined)`；上层 `detail.ux:553` `:585` 判 `!err` 即成功 | **1（最高）** |
| **P1-7** | 实验室「课程详情」入口不传 `detail_classId`，打开的是上一门课 / 空白兜底，是 P0-10 的主入口 | P1 | 🔴 未修（本轮新增） | `lab-list.js:21` + `lab.ux:101`；对比已修的 `class-list.js:127`、`week-view.ux:652` | **2** |
| **U-2** | T9 键盘缺 `jp.png`，切日文裂图（一个文件的事） | P1 | 🔴 未修 | `src/components/InputMethod/assets/t9/`（arc/full/horizontal 都有，t9 没有） | 3 |
| **U-7** | 方屏按钮热区 <40px（158 个），9 Pro / 10 Pro 都是方屏 | P1 | 🔴 未修 | `@media (shape: rect)` 下返回键 28×24 → ≥40×40 | 4 |
| **U-10** | 12 个页面无加载 / 空 / 错误三态 | P1 | 🔴 未修 | 全仓 | 5 |
| **U-9** | 彩色 emoji 当图标（84 处、21 种符号），缺字形显示豆腐块 | P1 | 🔴 未修 | 全仓 | 6 |
| **P1-3** | 圆屏四角控件裁切（9 Pro / 10 Pro 是方屏，本条只影响老款；真机确认后可关） | P1 | 🟡 部分改善 | shape 适配已 29/29，裁切大概率顺带解决 | 7（真机后定） |
| **P1-4** | 全站写死 px，`designWidth: "device-width"` | P1 | 🔴 未修 / 待你确认 | `manifest.json:57` | ⏸ 见下方「需要你回答」 |

**不进待办的**：P2-11 / P2-12 / P2-13 / P3-14（见报告，低优先）；P4 白嫖类见报告末尾「已知风险·不急修」，不排期。

---

## 上轮 9 条 P0 复核：零回归 ✅

| 编号 | 复核证据（本轮重扫） |
|---|---|
| P0-1 删课表串表 | `deleteScheduleAndShift` 调用 ≥8 处（`schedule-manager.ux:416` 等）✅ |
| P0-2 新增不显示 | `add-course.ux:505 insertCourse`；`insertCourse` 内 `invalidateCache` ✅ |
| P0-3 编辑清备注 | `detail.ux:559 notes: this.courseNotes \|\| ""` ✅ |
| P0-4 day 竞态 | `add-course.ux:485-487 if(!this.day) setDefaultDay()` ✅ |
| P0-5 备份漏主题 | `backup-restore.ux:76 "appTheme"` ✅ |
| P0-6 复制课表中间态 | 多处 fail 分支 + toast ✅ |
| P0-7 激活无反馈 | `activation.ux` 11 处 toast ✅ |
| P0-8 返回退不出 | 全仓 `onBackPress` 1 处（`index-full.ux:357`）✅ |
| P0-9 模块测试跳砖 | `lab-module-test` 全仓 0 处；28 个静态跳转目标 100% 命中 manifest ✅ |
| P1-1 / P1-5 / P1-6 / U-12 | shape 适配 29/29；`index-full.ux:52` 错误态 + 重试；`class-list.js:127` 已加 fail；`countdown-demo` 已删 ✅ |

---

## 你的上轮登记：已同步 ✅

`ef095da` 你把 P0-2 / P0-3 / P1-1 / P1-5 / U-12 从「②处理中 / ③打算做」移到了「①已处理」，②处理中区已清空 —— 与本轮代码复核**完全一致**，无差异。U-1 维持 ⏸，U-3 已改判 🔴 低优先。

---

## 操作链路走查速览（10 条）

**通 8 条**：首次启动 / 激活判断 / 新增课程 / 删除课程 / 本地存储读回 / 主界面渲染 / 设置持久化 / 页面入口按钮。
**不通 1 条**：**编辑已有课程**（主路径通，**实验室入口不通** → P0-10）。
**不适用 1 条**：数据同步（全仓 `fetch` = 0，纯本地）。

---

## 需要你回答的（2 件）

1. **P1-4**：`manifest.json` 的 `designWidth: "device-width"` 是快应用官方推荐写法，我倾向于**这不算缺陷**。你说一句「不算」我就标 ⚪ 永久关闭。
2. **U-1 输入法**（已问 **4 轮**）：真机上是 **A 完全唤不起 / B 偏右被切 / C 能出但打不出字**？你上轮说「待测试后回填」——回填一个字母即可，我据此修或关闭。

---

## 作者回复区（你填，我不动）

> 写在这儿，或 `git commit -m "fix(qa): P0-10 …"`。下轮我扫编号 + 读本区 + 开代码复核。

| 编号 | 你的结论（①已处理/②处理中/③打算做/③不打算做） | commit |
|---|---|---|
| P0-10 | | |
| P1-7 | | |
| U-2 | | |
| U-7 | | |
| P1-4 | | |
| U-1 真机现象（A/B/C） | | |

**其他想说的：**
