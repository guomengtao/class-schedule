# QA 第 10 轮交接 · 2026-09-12 18:53

> QA → 作者(Tom)。本轮**零代码变更**（HEAD `29c4178`，与第 9 轮复核基线 `012343d` 之间只有 1 个 `docs(qa):` 文档 commit）。
> 完整报告：[`../../../reports/class-schedule-QA-20260912-1853.md`](../../../reports/class-schedule-QA-20260912-1853.md)（仓库内：`docs/qa-reports/` 同目录历史报告）

## 本轮 HEAD

`29c4178` · v1.4.202 · versionCode 702

## 一句话结论

**P0 连续第 2 轮清零，走查 10 条「不通 = 0」**；上轮两条 P1（P1-9 / P1-10）**均未修**，且 P1-9 影响面本轮由 3 页**扩大到 4 页**。

---

## 本轮待办（只列 P0 / P0-S / P1）

| 编号 | 标题 | 优先级 | 状态 | 定位 | 建议顺序 |
|---|---|---|---|---|---|
| **P1-10** | 输入法 `adjustScreenWidth` 回调 `this` 丢失，屏宽恒 336 | P1 | 🔴 未修（第 2 轮） | `InputMethod.ux:808-817`（`:811`/`:814`）；触发点 `:456-457` | **1（一行修）** |
| **P1-9** | 4 个页面只有圆屏适配，缺 rect / pill | P1 | 🔴 未修（第 2 轮，本轮扩大） | `week-grid-demo.ux:247`、`week-grid-simple.ux:183`、`week-text-simple.ux:251`（rect/pill 全缺）；`chinese-input-full.ux`（有 pill、**缺 rect**） | **2** |
| P1-3 | 圆屏 modal 四角裁切 | P1 | ⏸ 待真机验证（已问 2 轮） | `e12a13c` 已合入 | — |
| P1-4 | `designWidth: device-width` 写死 px 无折算 | P1 | ⏸ 作者已接受未排期（已问 3 轮） | `manifest.json` | — |
| P1-8 | 零遥测 | P1 | 挂账 | — | — |

> P4（本地激活可绕过等）**按约定不写进待办表**，见报告末尾「已知风险·不急修」。

### 建议修复顺序与理由

1. **P1-10 先修**：只加一行 `var self = this` 并回调内改 `self.screenWidth`，零风险。
   - 影响面本轮已收敛：**9 Pro 屏宽 336×480，默认值恰好命中 → 9 Pro 无影响**；风险在 **10 Pro 与其他非 336 宽 rect 设备**。
2. **P1-9 其次**：4 页各补一段 `@media (shape: rect)`（+ `chinese-input-full` 只需补 rect，pill 已有），参照 `settings.ux` 写法。入口在实验室，非主路径，但 9 Pro / 10 Pro 都会踩到。

---

## 本轮新查出的两点（上轮没记录）

1. **P1-9 影响面扩大**：`chinese-input-full.ux` **有 pill 但缺 rect**，上轮只报了 3 页。精确口径重数：rect **25/29**、pill **26/29**（上轮记的 26/29 未区分这两者）。
2. **P2-11 精确根因**：`welcome.ux` 的 `_hasEntered` 前哨**还在**（`:53`），但两个进入方法 `doEnter`(`:119-131`) / `enterSchedule`(`:132-144`) **只在 `router.replace` 的回调里写它、进入前从不读** → 防重入锁名存实亡，连点两次会连发两次 `replace`。修法：方法首行 `if (this._hasEntered) return`，并在 `replace` **之前**置 `true`。

---

## 本轮确认「已修 / 别再报」

- **新增检查项通过**：`require("@system.*")` 6 种（storage/router/prompt/device/resident/app）**全部在 manifest 声明**，无遗漏。
- 历轮已修项**零回归**：`deleteScheduleAndShift` 8 / `onBackPress` 1 / `lab-module-test` 0 / `fetch(` 0 / `padding:44px` 29/29 / `invalidateCache` 9 / `database.js` `hit` 标记在（P0-10 未改回）/ 直写 `allCourses_` 0 / 箭头函数 0。
- 走查第 9 条「数据同步」继续标注**不适用**（手环全系不支持 `system.fetch`/`request`，应用 100% 本地数据）。

---

## 作者回复区（请填在下面，不要改上面的内容）

### ① 已处理（填 commit）

| 编号 | 一句话说明 | commit |
|---|---|---|
|  |  |  |

### ② 处理中

（无 / 填这里）

### ③ 待处理

| 编号 | 打算做 / 不打算做 | 理由 |
|---|---|---|
| P1-10 |  |  |
| P1-9 |  |  |
| P1-3（真机结果） |  |  |
| U-1 输入法真机现象（已问 6 轮） |  |  |
