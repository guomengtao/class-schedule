# 2026-09-19

## 标准版评分冲分（第八 ~ 第十一轮：96 → 100）

此前已完成：功能完整性 86→100（时间冲突检测 `database.checkDayConflict` + 空态引导）。

**第八轮** 胶囊屏文字可读性 84 → 100（总分 91 → 95）
- `week-view.ux`：新增 `device.getInfo` 屏型探测；胶囊屏下 `rowNumWidth → 0`（关行号列）、`cellWidth → min(w, 60)`。五模板列数由 1.57/2.29 修到 2.67~3.20（规范 2.5~3.5）
- `add-course.ux`：胶囊屏课程条 88px → 120px（课名 3 字 → 4.5 字，同类课名可区分）

**第九轮** 数据稳定性 90 → 100（零代码改动）
- 全量核查 41 处 `JSON.parse`：**已全部在 `try` 内**（database 9、store 7、auth-store 5、页面层 18、utils/pin-helper 2），P3-1 是文档清单未同步，实际早已修复

**第十轮** 字号设置文案修正，维度 8 归位
- `settings.ux`：标题「字体大小」→「**首页课程字号**」；新增说明「仅影响首页课程卡片，其他页面为固定字号」（20px / 行高 24px）
- 确认"只作用于首页"是有意设计 → 维度 8 由 82 归位 84

**第十一轮** 布局与触控 82 → 100、UI 一致性 84 → 100（**总分 100，八维度全满分**）
- 推翻"192px 物理宽度不可突破"旧判断：`index` 的总/今/明早已下移到 `.nav-toolbar`，胶囊屏 header 只剩 ◀ + 标题 + ▶（`160 − 48×2 = 64px` 够用）
- `detail`：header 改 `flex-wrap: wrap` + 标题 `order:1 / width:100%` 换第二行；轮播区改纵向（卡片 `order:0` 全宽，箭头 `order:1` 落第二行 48px）
- 提到 48px 的元素：`detail` back / trash / swipe-arrow / location-display；`index` nav-btn-circle / day-nav-circle / quick-add-scroll；`add-course` mgmt-btn；`course-manager` add-btn-toolbar；`week-view` wv-tpl / wv-back-wrapper

## 产出文档
- 新建 `docs/标准版100分评分标准.md`（守护手册：各维度达标条件 + 13 条红线 + 回退陷阱 + 自检流程）
- 同步更新 `docs/标准版完善度综合评分.md`（§0.8~§0.12、维度表、§3.4、§10 候选表等）

## 撰写最新评测报告
- 新建 `docs/标准版最新评测报告.md`：在 100/100 基础上做**批判性解读**
- 核心立场：100 分是「既定框架 + 静态走查」下的满分，不等于产品完美
  - 三大边界：框架边界（只评标准版）、方法边界（**未真机运行**，§九 18 项清单全未勾选）、口径边界（考规范符合度而非满意度）
- 水平定位：细分场景（手环快应用课程表）下「小而完整、边界清晰、适配到位」的成熟工具
- 结论：标准版边界内已达较好水平；后续重心从"冲分"转向 **验证 → 防回退 → 打磨体感**
- 未解决遗留项（报告已列）：`add-course` 时间步进 ±1（08:00→15:30 需点 15 次）、`course-manager-v2` 死页、`week-view` 基础样式 .wv-cell 32px/.wv-tpl 36px、胶囊屏竖向空间与符号渲染需真机

## 「添加课程」胶囊屏重新规划方案
- 新建 `docs/添加课程-胶囊屏重新规划方案.md`（10 章节：目标定位 / 现状问题 / UI 重设计 / 架构流程 / 时间滚轮 / 中文输入 / 胶囊屏适配 / 性能 / 异常 / 验收）
- 现状量化问题：时间步进 小时±1、分钟±5，内联展开 → 08:00→15:30 需 **15 次点击**；表单堆叠需滚动；位置每次必跳 `chinese-input`
- 核心方案：胶囊屏改**分步向导**（课程名 / 时间 / 星期 / 位置，每屏零滚动）+ **全屏滑动滚轮选时间** + **快捷时段**（15 次 → 1 次）+ 位置**选择优先、输入兜底**
- 关键技术前提：项目**无原生 picker**，滚轮须用 `<scroll scroll-y>` + 行高 48px + 吸附自研；
  **`scroll` 的偏移读取与程序化定位能力需先做真机最小验证**（不同固件可能有差异，已在文档标注）
- 滚轮参数：列宽 76×2（间隔 8，内容区 160）、行高 48px、3 行可见 144px、时 24 档 / 分 12 档（5 分粒度沿用）、有限列表（改进现状 23→0 循环）
- 吸附：onscroll 节流 80ms + 停止滚动 ~120ms 后定位一次，避免与惯性打架
- 保留能力：`checkDayConflict` 冲突检测、`isSaving` 锁、预设课程库、theme 体系、存储结构不变

## 修复「安装后无法退出」bug
- 真实根因（与 `docs/cannot-exit-analysis.md` 旧结论不同）：welcome/index 的 `onBackPress` 用 `try { app.exit() } catch(e){}` 静默吞异常后 **`return true`**；
  `return true` = 消费返回键、拦截系统默认行为 → exit 无效时**按返回键毫无反应** = 退不出去
- 修复：
  1. `src/data/utils.js` 新增 `exitApp()`，依次探测 `exit` / `terminate` / `finish` 三种退出入口
  2. 两处 `onBackPress` 改为 `return utils.exitApp() === true`（**成功才拦截，失败 return false 交还系统**）
  3. welcome 加显式「退出」按钮作为最后兜底，失败时 toast「请用系统手势退出应用」
  4. 顺带把 welcome 按钮提到 48px（胶囊屏原 40px、圆屏原 44px，此前未纳入核心页评估）
- 待真机确认：目标设备 `@system.app` 实际暴露哪个退出方法

## 制作「添加课程 V2」测试页（落地重新规划方案）
- 新建 `src/pages/add-course-v2/add-course-v2.ux`：4 步向导（课程名 / 时间 / 星期 / 位置）
  + 滑动滚轮（时 24 档 / 分 12 档，行高 48px，3 行可见 144px，两列各 76px + 冒号 8 = 160px 胶囊屏内容区）
  + 快捷时段 6 个（08:00/08:45/10:00/13:30/15:00/19:00），1 次点击 + 自动结束 +45min
  + 点击行选中兜底（吸附不可用时仍可用）；结束≤开始自动修正；冲突检测 + 二次确认
- `manifest.json` 注册 `pages/add-course-v2`（实验室课程组）；`settings.ux`「工具」组加入口「添加课程 V2」+ `openAddCourseV2()`
- 自检中自己发现并修复的 2 个缺陷：
  1. 列表原本是普通 div，13 项/7 项会溢出且**不可滚动** → 各 step 内容区改为 `<scroll scroll-y="true">`
  2. Step2 胶囊屏内容约 440px > 可用 368px → 快捷时段改**横向单行滚动**（省 114px），避免嵌套滚动冲突
- **未实现（测试版简化）**：中文输入兜底（自定义课程名/其他地点跳 chinese-input）、草稿恢复、从已有课程同步课程名与历史地点
- 待真机验证：`scroll` 的 `onscroll` 是否返回 `scrollTop`、`scrollTo` 是否可用（影响吸附）、吸附偏差 ≤半行、滚轮手感
- 测试路径（两条，都加了）：设置 → **工具**（需点击展开）→ 添加课程 V2；设置 → **测试区** → 列表首项「添加课程 V2」
- ⚠️ 注意：`settings.ux` 的入口曾一度**未持久化**（编辑后文件内容回退），已重新加入并 search 校验确认存在。以后改动关键入口后应 search 复核

## 测试区 UI 重构 + 官方组件调研
- 重构 `src/pages/test-area-v2/test-area-v2.ux`：卡片列表 → **官方 `list` / `list-item` 原生组件**平铺简单列表
  （左文字 24px + 右箭头 `›` + 底部分隔线，条目高 56px，rect 屏 64px；返回按钮由 image 改文字 `‹`，修掉原 `{{ iconTheme }}` 未定义导致的图标路径失效）
- **Vela 官方组件调研结论**（iot.mi.com/vela/quickapp/zh/components/）：
  - 容器组件：div、**list**、**list-item**、scroll、stack、swiper
  - 表单组件：input、**picker**、switch、slider
  - 基础组件：text/span/a/image/image-animator/progress/marquee/chart/qrcode/barcode
- **重要修正**：Vela **框架支持 `picker` 组件**。此前"项目无 picker"仅指项目代码没用过，不代表框架不支持。
  → 「添加课程 V2」的时间滚轮**可改用官方 `picker`**，可能比自研 scroll 滚轮更可靠、更贴近官方观感（需真机验证其形态）
- 官方提供的是**组件能力**与通用样式/颜色规范，**没有开箱即用的"默认列表视觉样式"**，视觉仍需自己写

## 首页/添加课程 UI 修正（用户反馈 4 项）
- **返回按钮显示叹号的根因**：`add-course.ux` 模板用 `{{ iconTheme }}/icon_back.png`，但 private **未定义 iconTheme**
  → 路径变成 `icons/undefined/icon_back.png` → 图片加载失败 → 显示占位叹号
  - 标准写法：`store.getTheme(function(t, themeName){ self.theme = t; self.updateIconSrc(themeName) })` + `updateIconSrc()` 设 iconTheme
  - 已修 `add-course`；**顺带发现并修了同类 bug**：`course-manager.ux`、`week-view.ux`（同样只使用未定义）
  - ⚠️ 今后新增页面若用 `{{ iconTheme }}` 图标，必须在 private 定义 + 主题回调赋值
- 首页底部加号按钮：实心 accent 色块 → **透明底 + accent 2px 描边**（用户反馈"背景不透明"）
- 课表切换图标：`icon_swap` → **`icon_chevron_down`**（下拉箭头表达"点击切换选择"；项目 icons 仅 9 个，无专用切换图标）
- 胶囊屏星期：单字「三」→「**周三**」（day-nav.js dayShortMap）
  - ⚠️ 同时发现 `index.ux` 有**第二处硬编码**缩写（约 L561，另一条代码路径），已同步改，否则部分路径仍显示单字
  - 宽度核算：胶囊屏 header 160 − 48×2 = 64px，「周三」2×26px = 52px ✅ 放得下
