# 2026-09-21

## manifest.json 修复
- 问题：`deviceTypeList` 里含 `"band"`，但小米 Vela 官方《项目配置》明确：可选值为 `watch` / `tv` / `car` / `phone`，**现只支持 watch**，`band` 不在合法枚举内（🟡 minor，配置取值合法性问题）。
- 修复：删除 `"band"`，`deviceTypeList` 现为 `["watch"]`；`node JSON.parse` 校验通过。
- 遗留（未改，待用户确认）：lint 提示第 34 行 `"name": "system.resident"` 不在 Vela feature 白名单（有效值含 system.app / system.device / system.router / system.storage 等），这是改动前就存在的警告，不在本轮指定范围内。
- 权威依据：https://iot.mi.com/vela/quickapp/zh/guide/framework/manifest.html

## manifest.json 复审（同日第二轮）
- 保持 `deviceTypeList: ["watch"]`：已二次核对官方文档，确认 `band` 非合法取值；中途曾误加回 `band` 已撤回
- 修复 `router._groups` 与 `router.pages` 不一致：课表视图补 `workday-info`；实验室工具移除未注册的 `test-area`（其 .ux 仍在，属废弃页，未注册故不可达）
- 校验：30 个注册页的 .ux 全部存在、JSON 合法、无分组漂移（`_groups` 与 `pages` 双向比对脚本）
- 保留项（lint 警告属误报/权衡，非缺陷）：`system.resident`（app.ux 真实调用，try/catch 包裹）、`hapjs.permission.DEVICE_INFO`（activation/device-info 调用 getDeviceId/getSerial）
- 未改建议：`config.logLevel` 现为 `log`（真机调试需要）；若出正式包可临时降为 `warn`，因 index 模块有高频 console.log
- 遗留提醒：新建的 `src/pages/workday-info/workday-info.ux` 目前是脚手架（"欢迎打开详情页"），无返回按钮，需补 header + 调课说明内容
