# 2026-09-22

## 代码丢失排查：2026 假期数据找回
- 用户报告 `src/data/holidays-2026.js`（放假/调休数据）丢失。排查结论：**不是"AI 改完没保存关 IDE"**，AI 写文件是直接落盘的。
- 真因 A：`~/.zsh_history` 里有 `git reset HEAD && git clean -fd && git status --short`，把曾 `git add` 过但未 commit 的文件从工作区删除（无回收站）。
- 真因 B：提交 `6e1fa0b "Release v1.6.49"` 零新增、删了 `docs/` 下 **265 个 md**（AI/脚本过度清理并被 commit 固化）。
- 恢复手段：
  - `git fsck --lost-found` + 逐个 blob grep "2026-01-01" → 命中悬空 blob `837fbefa4ef5e886beb7fd2ec681a30e90a66304`（3215 字节），`git cat-file -p` 导出为 `src/data/holidays-2026.js`（33 个假期 + 6 个调休，node require 校验通过）
  - `git checkout HEAD~1 -- docs` 恢复 265 个文档（229 个顶层 md），目前在暂存区，用户确认后再 commit
- 产出文档：`docs/代码丢失排查与防护（2026假期数据找回）.md`（含证据链、根因、抢救命令速查、假期数据全文备份）
- 规则固化：`.codebuddy/rules/总体规则.mdc` 新增"禁止 git clean -fd/-fdx、git reset --hard、rm -rf 与目录清理；改完提醒用户尽快 commit"
- 注意：`src/data/holidays-2026.js` 目前**未被任何代码 require**；假期真值仍走 storage 键 `holiday_data`（`src/pages/index/modules/holiday.js` 读取，workday-info 页写入）。下一步可做"首次启动写入内置数据 + 允许重置为官方默认"。

## 假期数据接入（已完成，commit bcdccd6）
- 数据恢复与 265 个 docs 已提交（commit 0c68a0f），工作区干净，0 未跟踪文件
- 新增 `src/data/holiday-preset.js`：把 `holidays-2026.js` 转成 storage `holiday_data` 结构（holiday→{name,greeting}；workday→{dayName,weekDay}），并打 `builtin:true`
  - `ensurePreset()`：读版本键 `holiday_preset_version`（值 `builtin-2026-1`），不一致时**只补缺失日期**（不覆盖用户改动），写完更新版本；已验证幂等
  - `restoreDefault()`：用内置数据覆盖内置日期（39 条），**保留用户自定义日期**；带二次确认弹窗
- `app.ux` onCreate 新增 `migrateBuiltinHolidays()`；`src/pages/index/modules/holiday.js` 用 `ensurePresetOnce` 兜底（防止首页早于 app.ux 写盘时读空），抽出 `readHolidayState/applyEntry/resetState` 三个函数
- `workday-info.ux` 新增「内置官方放假安排」区块：显示放假/调休天数 + 「恢复 / 更新内置数据」按钮（48px，胶囊屏安全），点击后二次确认再执行 toast 提示
- 验证：`node --check` 全通过；用 fake storage 跑通「首次补齐 39 条 → 用户改+自定义 → 一键恢复覆盖内置、保留自定义 → 二次 ensure 补 0」

## 数据分层结论（回答用户）
- 本项目**不存在真正的数据库**：`src/data/database.js` 用的是 `@system.storage`（键 `allCourses`）+ 内存缓存 `_cache` 自实现的 CRUD；假期数据是另一个键 `holiday_data`
- `holidays-2026.js` 不是数据库，而是**打包进 App 的内置种子数据源**（只读常量）；流程：启动时 `ensurePreset()` 把种子补齐进 storage → 页面只读写 storage → 「恢复内置数据」用种子覆盖内置日期但保留用户自定义
- 校验脚本套路：`node -e` 里劫持 `Module.prototype.require` 注入 fake `@system.storage`，可本地跑通 storage 逻辑

## 节假日规则说明（commit 3a17a8a）
- `workday-info.ux` 新增「节假日规则与来源」可展开卡片，`RULE_LINES` 数组含 6 条：官方文件号、放假/调休处理逻辑、`targetWeekDay` 取值与"国务院不规定补课星期、属教育系统通行惯例"的说明、可自定义覆盖
- 首页调休提示条的「?」改为 `router.push({uri:"/pages/workday-info", params:{showRules:"1"}})`；目标页用 `public: { showRules: "" }` 接参（hapjs 参数只能由 public/data 同名属性接收），`applyRulesParam()` 命中即展开
- 注意：首次在此项目使用 `public:` 声明 + 路由 params 传值，后续页面如需跨页传参沿用此写法

## 调休页（问号入口）优化（commit 6417e92）
- 胶囊屏标题截断修复：`workday-info` 头部删掉 44px 右侧占位块，`.header-title` 字号 24→18px，padding 收窄到 12px 10px，返回按钮加 4px 右边距
  - 192px 宽度下可用宽度从 80px → 124px，"节假日与调休"6 字×18px=108px 可完整显示
- 新增「2026年调休安排」卡片：`loadWorkdayPlan()` 优先读 storage `holiday_data`，读不到回落 `holidayPreset.buildEntries()`；`buildWorkdayPlan()` 过滤本年度 `type==='workday'`，渲染格式「1月4日（星期日）→ 按星期一课表」，非内置条目追加「（自定义）」，按日期升序
  - 实测输出 6 条：1/4→周一、2/14→周一、2/28→周五、5/9→周二、9/20→周二、10/10→周三
- 规则说明改为默认展开（`rulesExpanded: true`），文案扩到 7 条：含数据来源（国办发明电〔2025〕7号）、放假/调休显示逻辑、**判定链路**（读日期→查 holiday_data→取 weekDay 加载课程）、targetWeekDay 取值与"国务院不规定补课星期"惯例、可自定义覆盖
- 本地校验套路：临时脚本 mock `@system.storage/@system.router/@system.prompt/store.js`，用 `new Function("require", script+";return page;")(require)` 把 ux 的 `<script>` 块跑起来直接测页面方法（注意 `new Function` 作用域没有 require，必须显式传入）

## Git 安全边界（已向用户说明）
- 已 commit 的内容：对象进 `.git/objects`，后续删除/修改都不影响历史版本，可用 `git log -- <path>` / `git checkout HEAD~1 -- <path>` 取回
- 残余风险点（必须在常识上提醒）：本地未 push 时仓库物理损坏；`--force push` 或删远程分支；悬空对象（只 add 未 commit）会被 `git gc/prune` 在默认 90 天后回收；`.git` 被 rm
- 真正高危的始终是：**从未 add/add 后被 reset 出来的未跟踪文件**，`git clean -fd` 一次清空且无回收站
