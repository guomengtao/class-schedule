# 胶囊屏独立设置隐藏设置栏目的描述信息问题分析

## 问题现象

在胶囊屏（capsule screen）设备上，设置页面（`settings.ux`）中所有栏目的描述/提示信息（`row-hint`）均不显示，包括"首页设置"栏目的"栏目时间"描述、以及其他栏目如"云端备份"、"实验性功能"等的提示文本。

## 根因定位

### 问题代码位置

**文件**: [settings.ux](file:///Users/Banner/Documents/guomengtao/tom/class/class/src/pages/settings/settings.ux#L841-L843)

```css
@media (shape: capsule) {
  /* ... 其他样式 ... */
  .row-hint {
    display: none;   /* <-- 此处将胶囊屏下所有描述信息隐藏 */
  }
}
```

### 影响范围

该 CSS 规则以 `display: none` 全局隐藏了 `.row-hint` 类，导致胶囊屏下**所有**设置栏目的描述文本全部不可见，共涉及 **11 处** `row-hint` 元素：

| 行号 | 栏目名称 | 描述信息 | 作用 |
|------|---------|---------|------|
| 14 | 昵称 | `点击设置` | 提示用户未设置昵称时可点击设置 |
| 34 | 字体大小 | `{{ displaySize }}px` | 显示当前字体大小数值 |
| 54 | 默认首页 | `{{ defaultHomepageLabel }}` | 显示当前默认首页配置 |
| 96 | 课表风格 | `{{ currentTemplateName }}` | 显示当前课表模板名称 |
| 106 | **首页设置** | **`栏目时间`** | **说明该设置用于配置首页栏目和时间** |
| 114 | 数据备份 | `云端备份` | 说明备份方式为云端备份 |
| 122 | 功能实验室 | `实验性功能` | 说明该入口为实验性功能 |
| 130 | 高级版 | `解锁` | 提示可解锁高级功能 |
| 138 | 打赏支持 | `支持` | 说明该入口为支持打赏 |
| 146 | 黑屏检测 | `诊断` | 说明该功能用于诊断 |
| 156 | 恢复默认 | `重置` | 说明该操作为重置 |

## 原因分析

### 1. 设计意图推测

开发者可能出于以下考虑设置了 `display: none`：

- **胶囊屏宽度有限**：胶囊屏是窄长形状，水平空间紧张
- **节省空间**：隐藏描述文本可以为标签文字腾出更多空间
- **减少视觉噪音**：认为描述文本在窄屏上显得拥挤

### 2. 问题所在

- **一刀切的隐藏策略**：将所有 `.row-hint` 统一隐藏，不分场景
- **丢失关键信息**：像"栏目时间"这样的描述信息对用户理解设置功能至关重要
- **不一致的体验**：圆形屏（circle）和方屏（rect）正常显示描述，唯独胶囊屏不显示

### 3. 与其他屏幕形态的对比

| 屏幕形态 | `.row-hint` 样式 | 描述信息状态 |
|---------|-----------------|------------|
| 圆形屏 (circle) | `font-size: 24px;` | ✅ 正常显示 |
| 胶囊屏 (capsule) | `display: none;` | ❌ 完全隐藏 |
| 方屏 (rect) | `font-size: 24px;` | ✅ 正常显示 |

### 4. `showLabSection` 缺失问题

另外，在 [homepage-settings.ux](file:///Users/Banner/Documents/guomengtao/tom/class/class/src/pages/homepage-settings/homepage-settings.ux) 页面中，`showLabSection` 字段虽然在 [store.js](file:///Users/Banner/Documents/guomengtao/tom/class/class/src/data/store.js#L671) 和 [storage-tables.js](file:///Users/Banner/Documents/guomengtao/tom/class/class/src/data/storage-tables.js#L83) 中定义，但在 `homepage-settings.ux` 页面的 UI 中**没有任何对应的开关控件**，这意味着：

- 用户无法通过 UI 控制"实验室栏目"在首页的显示/隐藏
- 该配置项只能通过代码或直接操作存储来修改
- 默认值为 `false`（不显示实验室栏目）

## 建议修复方案

### 方案一：缩小字体而非隐藏（推荐）

将 `display: none` 改为缩小字体，保持信息可见：

```css
@media (shape: capsule) {
  .row-hint {
    font-size: 18px;      /* 缩小字体适应窄屏 */
    max-lines: 1;          /* 限制单行 */
    text-overflow: ellipsis; /* 溢出省略 */
  }
}
```

### 方案二：选择性隐藏

根据栏目重要性，只隐藏次要描述，保留关键信息：

```css
@media (shape: capsule) {
  .row-hint {
    font-size: 18px;
  }
  /* 对昵称这种条件显示的保持可见 */
  /* 对字体大小数值等关键信息保持可见 */
}
```

### 方案三：增加 `showLabSection` 开关

在 `homepage-settings.ux` 中增加"实验室栏目"的开关控件，使配置完整可用：

```html
<div class="item-row">
  <text class="item-label">实验室栏目</text>
  <div class="switch-track" onclick="toggleLabSection">
    <div class="switch-thumb"></div>
  </div>
</div>
```

## 总结

| 项目 | 内容 |
|------|------|
| 问题根因 | `settings.ux` 胶囊屏 CSS 中 `.row-hint { display: none }` 全局隐藏描述文本 |
| 影响范围 | 11 处描述信息全部不可见 |
| 次要问题 | `showLabSection` 配置项在 homepage-settings 页面缺少 UI 开关 |
| 修复优先级 | 高 - 影响所有胶囊屏用户的设置体验 |