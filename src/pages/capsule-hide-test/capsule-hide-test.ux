<template>
  <div class="page" style="background-color: {{ theme.bg }}">
    <div class="back-header">
      <input class="back-btn" type="button" value="<" onclick="goBack"
             style="background-color: {{ theme.card }}; color: {{ theme.accent }}" />
      <text class="header-title" style="color: {{ theme.text }}">胶囊屏隐藏测试</text>
    </div>

    <text class="section-hint" style="color: {{ theme.textSecondary }}">胶囊屏隐藏 → 方屏可见</text>

    <div class="list-scroll">
      <div class="list-row capsule-hide" style="background-color: {{ theme.card }}">
        <text class="row-name" style="color: {{ theme.text }}">a</text>
        <div class="row-right">
          <text class="row-desc" style="color: {{ theme.textMuted }}">胶囊屏隐藏此项</text>
          <text class="row-arrow" style="color: {{ theme.textMuted }}">></text>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
const store = require("../../data/store.js")
import router from "@system.router"

export default {
  private: {
    theme: {
      bg: "#1a1a2e",
      card: "#16213e",
      accent: "#7ec8e3",
      text: "#ffffff",
      textSecondary: "#888899",
      textMuted: "#555566"
    }
  },

  onInit() {
    var self = this
    store.getTheme(function(t) { self.theme = t })
  },

  onShow() {
    var self = this
    store.getTheme(function(t) { self.theme = t })
  },

  goBack() {
    router.back()
  }
}
</script>

<style>
.page {
  width: 100%;
  height: 100%;
  flex-direction: column;
  padding: 44px 16px 16px 16px;
}

.back-header {
  flex-direction: row;
  align-items: center;
  margin-bottom: 16px;
}

.back-btn {
  width: 48px;
  height: 36px;
  border-radius: 8px;
  font-size: 20px;
  text-align: center;
  flex-shrink: 0;
}

.header-title {
  font-size: 36px;
  font-weight: bold;
  margin-left: 12px;
  line-height: 44px;
}

.section-hint {
  font-size: 28px;
  margin-bottom: 12px;
  line-height: 38px;
}

.list-scroll {
  flex: 1;
  flex-direction: column;
  overflow: scroll;
}

.list-row {
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  border-radius: 12px;
  padding: 16px;
  margin-bottom: 8px;
  min-height: 56px;
}

.row-name {
  font-size: 32px;
  flex-shrink: 0;
  max-lines: 1;
  line-height: 38px;
}

.row-right {
  flex-direction: row;
  align-items: center;
  flex-shrink: 1;
  overflow: hidden;
}

.row-desc {
  font-size: 28px;
  margin-right: 6px;
  max-lines: 1;
  line-height: 34px;
}

.row-arrow {
  font-size: 28px;
  flex-shrink: 0;
}

@media (shape: capsule) {
  .capsule-hide {
    display: none;
  }
}

@media (shape: circle) {
  .page {
    padding: 44px 36px 44px 36px;
  }
  .back-header {
    margin-bottom: 16px;
  }
  .back-btn {
    width: 52px;
    height: 40px;
    font-size: 24px;
  }
  .header-title {
    font-size: 36px;
    line-height: 48px;
  }
  .list-row {
    padding: 18px 16px;
    margin-bottom: 10px;
  }
}

@media (shape: rect) {
  .page {
    padding: 44px 16px 16px 16px;
  }
  .back-btn {
    width: 50px;
    height: 40px;
    font-size: 24px;
  }
  .header-title {
    font-size: 36px;
    line-height: 48px;
  }
}
</style>
