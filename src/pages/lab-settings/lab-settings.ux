<template>
  <div class="page" style="background-color: {{ theme.bg }}">
    <div class="back-header">
      <text class="back-btn" onclick="goBack" style="background-color: {{ theme.card }}; color: {{ theme.accent }}"><</text>
      <text class="header-title" style="color: {{ theme.text }}">实验室</text>
    </div>

    <text class="section-hint" style="color: {{ theme.textSecondary }}">实验性功能列表</text>

    <div class="list-scroll">
      <div class="list-row" for="(idx, item) in labItems" style="background-color: {{ theme.card }}" onclick="onOpenItem(idx)">
        <text class="row-name" style="color: {{ theme.text }}">{{ item.name }}</text>
        <div class="row-right">
          <text class="row-desc" style="color: {{ theme.textMuted }}">{{ item.desc }}</text>
          <text class="row-arrow" style="color: {{ theme.textMuted }}">></text>
        </div>
      </div>
    </div>

    <text class="version-text" style="color: {{ theme.textMuted }}">v{{ versionText }}</text>
  </div>
</template>

<script>
import router from "@system.router"
const store = require("../../data/store.js")

export default {
  private: {
    labItems: [
      { name: "添加课程V2", desc: "全新添加课程页面", uri: "/pages/add-course-v2" },
      { name: "添加课程(胶囊)", desc: "胶囊版添加课程页面", uri: "/pages/lab-add-course" },
      { name: "震动实验室", desc: "震动效果测试", uri: "/pages/vibration-lab" },
      { name: "二维码生成", desc: "二维码生成工具", uri: "/pages/qrcode-generator" }
    ],
    versionText: "",
    theme: {
      bg: "#1a1a2e",
      card: "#16213e",
      accent: "#7ec8e3",
      text: "#ffffff",
      textSecondary: "#888899",
      textMuted: "#555566"
    }
  },

  onInit() {
    var self = this
    store.getTheme(function(t) { self.theme = t })
    this.loadVersion()
  },

  onShow() {
    var self = this
    store.getTheme(function(t) { self.theme = t })
  },

  loadVersion() {
    try {
      var v = require("../../data/version.js")
      this.versionText = v.version || "1.0.0"
    } catch (e) {
      this.versionText = "1.0.0"
    }
  },

  onOpenItem(idx) {
    var item = this.labItems[idx]
    if (item && item.uri) {
      router.push({ uri: item.uri })
    }
  },

  goBack() {
    router.back()
  }
}
</script>

<style>
.page {
  width: 100%;
  height: 100%;
  flex-direction: column;
  padding: 44px 16px 16px 16px;
}

.back-header {
  flex-direction: row;
  align-items: center;
  margin-bottom: 16px;
}

.back-btn {
  width: 48px;
  height: 36px;
  border-radius: 8px;
  font-size: 20px;
  text-align: center;
  flex-shrink: 0;
}

.header-title {
  font-size: 36px;
  font-weight: bold;
  margin-left: 12px;
  line-height: 44px;
}

.section-hint {
  font-size: 28px;
  margin-bottom: 12px;
  line-height: 38px;
}

.list-scroll {
  flex: 1;
  flex-direction: column;
  overflow: scroll;
}

.list-row {
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  border-radius: 12px;
  padding: 16px;
  margin-bottom: 8px;
  min-height: 56px;
}

.row-name {
  font-size: 32px;
  flex-shrink: 0;
  max-lines: 1;
  line-height: 38px;
}

.row-right {
  flex-direction: row;
  align-items: center;
  flex-shrink: 1;
  overflow: hidden;
}

.row-desc {
  font-size: 28px;
  margin-right: 6px;
  max-lines: 1;
  line-height: 34px;
}

.row-arrow {
  font-size: 28px;
  flex-shrink: 0;
}

.version-text {
  font-size: 24px;
  text-align: center;
  padding-top: 12px;
  flex-shrink: 0;
  line-height: 34px;
}

@media (shape: capsule), (shape: pill-shaped) {
  .header {
    height: 48px;
    margin-bottom: 8px;
    flex-direction: row;
    align-items: center;
  }
  .back-btn {
    width: 48px;
    height: 40px;
    border-radius: 20px;
    font-size: 24px;
    text-align: center;
    line-height: 40px;
    flex-shrink: 0;
  }
  .title {
    font-size: 28px;
    line-height: 44px;
    text-align: center;
    flex: 1;
    lines: 1;
    margin: 0 4px;
  }

}

@media (shape: circle) {
  .page {
    padding: 44px 36px 44px 36px;
  }
  .back-header {
    margin-bottom: 16px;
  }
  .back-btn {
    width: 52px;
    height: 40px;
    font-size: 24px;
  }
  .header-title {
    font-size: 36px;
    line-height: 48px;
  }
  .list-row {
    padding: 18px 16px;
    margin-bottom: 10px;
  }
}

@media (shape: rect) {
  .page {
    padding: 44px 16px 16px 16px;
  }
  .back-btn {
    width: 50px;
    height: 40px;
    font-size: 24px;
  }
  .header-title {
    font-size: 36px;
    line-height: 48px;
  }
}
</style>