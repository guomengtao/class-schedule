<template>
  <div class="debug-page" style="background-color: #1a1a2e">
    <div class="debug-header">
      <text class="debug-title" style="color: #7ec8e3">白屏排查测试 - Step 1</text>
    </div>
    <div class="debug-body">
      <text class="debug-ok" style="color: #00ff88">✅ 页面主体正常渲染</text>
      <text class="debug-hint" style="color: #888899">imports 已加回：router, vibrator, prompt, device, store, database</text>
    </div>
  </div>
</template>

<script>
import router from "@system.router"
import vibrator from "@system.vibrator"
import prompt from "@system.prompt"
import device from "@system.device"
const store = require("../../data/store.js")
const database = require("../../data/database.js")

export default {
  private: {
    test: "hello"
  },
  onInit() {
    console.log("[INDEX-DEBUG-STEP1] onInit executed")
    console.log("[INDEX-DEBUG-STEP1] store keys:", Object.keys(store))
    console.log("[INDEX-DEBUG-STEP1] database keys:", Object.keys(database))
  },
  onShow() {
    console.log("[INDEX-DEBUG-STEP1] onShow executed")
  }
}
</script>

<style>
.debug-page {
  flex-direction: column;
  min-height: 100%;
  height: 100%;
  padding: 44px 8px 8px 8px;
}

.debug-header {
  padding: 20px;
  align-items: center;
}

.debug-title {
  font-size: 24px;
  font-weight: bold;
}

.debug-body {
  flex: 1;
  align-items: center;
  justify-content: center;
  padding: 40px;
}

.debug-ok {
  font-size: 22px;
  font-weight: bold;
  margin-bottom: 12px;
}

.debug-hint {
  font-size: 14px;
}
</style>