<template>
  <div class="page" style="background-color: {{ theme.bg }}">
    <div class="back-header">
      <input class="back-btn" type="button" value="◀ 返回" onclick="goBack" style="background-color: {{ theme.card }}; color: {{ theme.accent }}" />
      <text class="header-title" style="color: {{ theme.text }}">高级版</text>
    </div>

    <scroll class="page-scroll" scroll-y="{{true}}">
      <div class="status-card" style="background-color: {{ theme.card }}">
        <div class="status-row">
          <text class="status-icon" style="color: {{ statusColor }}">[</text>
          <text class="status-text" style="color: {{ statusColor }}">{{ statusText }}</text>
          <text class="status-icon" style="color: {{ statusColor }}">]</text>
        </div>
        <div class="status-details" if="{{ statusDetail }}">
          <text class="status-detail-line" style="color: {{ theme.textSecondary }}">{{ statusDetail }}</text>
        </div>
      </div>

      <text class="section-title" style="color: {{ theme.text }}">激活软件</text>

      <div class="step-card" style="background-color: {{ theme.card }}">
        <div class="step-header">
          <div class="step-badge" style="background-color: {{ theme.accent }}">
            <text class="step-badge-text" style="color: {{ theme.bg }}">1</text>
          </div>
          <text class="step-title" style="color: {{ theme.text }}">购买高级版兑换码</text>
        </div>
        <text class="step-desc" style="color: {{ theme.textSecondary }}">在爱发电购买高级版兑换码，永久解锁全部高级功能，无后续费用</text>
        <div class="qr-section">
          <div class="qr-wrapper" style="background-color: #ffffff">
            <qrcode value="{{ afdianUrl }}" style="color: #000000; background-color: #ffffff; width: 120px; height: 120px;"></qrcode>
          </div>
          <text class="qr-label" style="color: {{ theme.textSecondary }}">扫码打开爱发电</text>
        </div>
        <div class="url-box" style="background-color: {{ theme.bg }}">
          <text class="url-text" style="color: {{ theme.accent }}">{{ afdianUrl }}</text>
        </div>
      </div>

      <div class="step-arrow" style="color: {{ theme.textMuted }}"><text>▼</text></div>

      <div class="step-card" style="background-color: {{ theme.card }}">
        <div class="step-header">
          <div class="step-badge" style="background-color: {{ theme.accent }}">
            <text class="step-badge-text" style="color: {{ theme.bg }}">2</text>
          </div>
          <text class="step-title" style="color: {{ theme.text }}">获取激活码</text>
        </div>
        <text class="step-desc" style="color: {{ theme.textSecondary }}">购买后扫描下方二维码，输入兑换码即可获取18位激活码</text>
        <div class="device-row">
          <text class="device-label" style="color: {{ theme.textSecondary }}">本机设备ID</text>
          <text class="device-value" style="color: {{ theme.accent }}">{{ deviceId || '获取中...' }}</text>
        </div>
        <div class="qr-section" if="{{ qrText }}">
          <div class="qr-wrapper" style="background-color: #ffffff">
            <qrcode value="{{ qrText }}" style="color: #000000; background-color: #ffffff; width: 120px; height: 120px;"></qrcode>
          </div>
          <text class="qr-label" style="color: {{ theme.textSecondary }}">扫码兑换激活码</text>
        </div>
        <div class="qr-section" if="{{ !qrText }}">
          <text class="qr-loading" style="color: {{ theme.textMuted }}">正在生成二维码...</text>
        </div>
        <div class="url-box" if="{{ qrText }}" style="background-color: {{ theme.bg }}">
          <text class="url-text" style="color: {{ theme.textMuted }}">{{ qrText }}</text>
        </div>
      </div>

      <div class="step-arrow" style="color: {{ theme.textMuted }}"><text>▼</text></div>

      <div class="step-card" style="background-color: {{ theme.card }}">
        <div class="step-header">
          <div class="step-badge" style="background-color: {{ theme.accent }}">
            <text class="step-badge-text" style="color: {{ theme.bg }}">3</text>
          </div>
          <text class="step-title" style="color: {{ theme.text }}">输入激活码</text>
        </div>
        <text class="step-desc" style="color: {{ theme.textSecondary }}">请输入18位激活码，逐位点选输入</text>

        <div class="cells-grid" style="background-color: {{ theme.card }}">
          <div class="cells-row">
            <div class="cell {{ cursor === 0 ? 'cell-active' : '' }}" onclick="onCellClick(0)" style="background-color: {{ cursor === 0 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 0 ? theme.bg : theme.text }}">{{ cell0 !== null ? cell0 : '-' }}</text>
            </div>
            <div class="cell {{ cursor === 1 ? 'cell-active' : '' }}" onclick="onCellClick(1)" style="background-color: {{ cursor === 1 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 1 ? theme.bg : theme.text }}">{{ cell1 !== null ? cell1 : '-' }}</text>
            </div>
            <div class="cell {{ cursor === 2 ? 'cell-active' : '' }}" onclick="onCellClick(2)" style="background-color: {{ cursor === 2 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 2 ? theme.bg : theme.text }}">{{ cell2 !== null ? cell2 : '-' }}</text>
            </div>
            <div class="cell {{ cursor === 3 ? 'cell-active' : '' }}" onclick="onCellClick(3)" style="background-color: {{ cursor === 3 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 3 ? theme.bg : theme.text }}">{{ cell3 !== null ? cell3 : '-' }}</text>
            </div>
            <div class="cell {{ cursor === 4 ? 'cell-active' : '' }}" onclick="onCellClick(4)" style="background-color: {{ cursor === 4 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 4 ? theme.bg : theme.text }}">{{ cell4 !== null ? cell4 : '-' }}</text>
            </div>
            <div class="cell {{ cursor === 5 ? 'cell-active' : '' }}" onclick="onCellClick(5)" style="background-color: {{ cursor === 5 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 5 ? theme.bg : theme.text }}">{{ cell5 !== null ? cell5 : '-' }}</text>
            </div>
          </div>
          <div class="cells-row">
            <div class="cell {{ cursor === 6 ? 'cell-active' : '' }}" onclick="onCellClick(6)" style="background-color: {{ cursor === 6 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 6 ? theme.bg : theme.text }}">{{ cell6 !== null ? cell6 : '-' }}</text>
            </div>
            <div class="cell {{ cursor === 7 ? 'cell-active' : '' }}" onclick="onCellClick(7)" style="background-color: {{ cursor === 7 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 7 ? theme.bg : theme.text }}">{{ cell7 !== null ? cell7 : '-' }}</text>
            </div>
            <div class="cell {{ cursor === 8 ? 'cell-active' : '' }}" onclick="onCellClick(8)" style="background-color: {{ cursor === 8 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 8 ? theme.bg : theme.text }}">{{ cell8 !== null ? cell8 : '-' }}</text>
            </div>
            <div class="cell {{ cursor === 9 ? 'cell-active' : '' }}" onclick="onCellClick(9)" style="background-color: {{ cursor === 9 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 9 ? theme.bg : theme.text }}">{{ cell9 !== null ? cell9 : '-' }}</text>
            </div>
            <div class="cell {{ cursor === 10 ? 'cell-active' : '' }}" onclick="onCellClick(10)" style="background-color: {{ cursor === 10 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 10 ? theme.bg : theme.text }}">{{ cell10 !== null ? cell10 : '-' }}</text>
            </div>
            <div class="cell {{ cursor === 11 ? 'cell-active' : '' }}" onclick="onCellClick(11)" style="background-color: {{ cursor === 11 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 11 ? theme.bg : theme.text }}">{{ cell11 !== null ? cell11 : '-' }}</text>
            </div>
          </div>
          <div class="cells-row">
            <div class="cell {{ cursor === 12 ? 'cell-active' : '' }}" onclick="onCellClick(12)" style="background-color: {{ cursor === 12 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 12 ? theme.bg : theme.text }}">{{ cell12 !== null ? cell12 : '-' }}</text>
            </div>
            <div class="cell {{ cursor === 13 ? 'cell-active' : '' }}" onclick="onCellClick(13)" style="background-color: {{ cursor === 13 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 13 ? theme.bg : theme.text }}">{{ cell13 !== null ? cell13 : '-' }}</text>
            </div>
            <div class="cell {{ cursor === 14 ? 'cell-active' : '' }}" onclick="onCellClick(14)" style="background-color: {{ cursor === 14 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 14 ? theme.bg : theme.text }}">{{ cell14 !== null ? cell14 : '-' }}</text>
            </div>
            <div class="cell {{ cursor === 15 ? 'cell-active' : '' }}" onclick="onCellClick(15)" style="background-color: {{ cursor === 15 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 15 ? theme.bg : theme.text }}">{{ cell15 !== null ? cell15 : '-' }}</text>
            </div>
            <div class="cell {{ cursor === 16 ? 'cell-active' : '' }}" onclick="onCellClick(16)" style="background-color: {{ cursor === 16 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 16 ? theme.bg : theme.text }}">{{ cell16 !== null ? cell16 : '-' }}</text>
            </div>
            <div class="cell {{ cursor === 17 ? 'cell-active' : '' }}" onclick="onCellClick(17)" style="background-color: {{ cursor === 17 ? theme.accent : theme.bg }}; border-color: {{ theme.border }}">
              <text class="cell-text" style="color: {{ cursor === 17 ? theme.bg : theme.text }}">{{ cell17 !== null ? cell17 : '-' }}</text>
            </div>
          </div>
        </div>

        <div class="keypad" style="background-color: {{ theme.card }}">
          <div class="keypad-row">
            <div class="keypad-btn" onclick="onKeyPress(1)" style="background-color: {{ theme.bg }}; border-color: {{ theme.border }}">
              <text class="keypad-text" style="color: {{ theme.text }}">1</text>
            </div>
            <div class="keypad-btn" onclick="onKeyPress(2)" style="background-color: {{ theme.bg }}; border-color: {{ theme.border }}">
              <text class="keypad-text" style="color: {{ theme.text }}">2</text>
            </div>
            <div class="keypad-btn" onclick="onKeyPress(3)" style="background-color: {{ theme.bg }}; border-color: {{ theme.border }}">
              <text class="keypad-text" style="color: {{ theme.text }}">3</text>
            </div>
          </div>
          <div class="keypad-row">
            <div class="keypad-btn" onclick="onKeyPress(4)" style="background-color: {{ theme.bg }}; border-color: {{ theme.border }}">
              <text class="keypad-text" style="color: {{ theme.text }}">4</text>
            </div>
            <div class="keypad-btn" onclick="onKeyPress(5)" style="background-color: {{ theme.bg }}; border-color: {{ theme.border }}">
              <text class="keypad-text" style="color: {{ theme.text }}">5</text>
            </div>
            <div class="keypad-btn" onclick="onKeyPress(6)" style="background-color: {{ theme.bg }}; border-color: {{ theme.border }}">
              <text class="keypad-text" style="color: {{ theme.text }}">6</text>
            </div>
          </div>
          <div class="keypad-row">
            <div class="keypad-btn" onclick="onKeyPress(7)" style="background-color: {{ theme.bg }}; border-color: {{ theme.border }}">
              <text class="keypad-text" style="color: {{ theme.text }}">7</text>
            </div>
            <div class="keypad-btn" onclick="onKeyPress(8)" style="background-color: {{ theme.bg }}; border-color: {{ theme.border }}">
              <text class="keypad-text" style="color: {{ theme.text }}">8</text>
            </div>
            <div class="keypad-btn" onclick="onKeyPress(9)" style="background-color: {{ theme.bg }}; border-color: {{ theme.border }}">
              <text class="keypad-text" style="color: {{ theme.text }}">9</text>
            </div>
          </div>
          <div class="keypad-row">
            <div class="keypad-btn keypad-clear" onclick="onClear" style="background-color: {{ theme.bg }}; border-color: {{ theme.border }}">
              <text class="keypad-text" style="color: #e74c3c">C</text>
            </div>
            <div class="keypad-btn" onclick="onKeyPress(0)" style="background-color: {{ theme.bg }}; border-color: {{ theme.border }}">
              <text class="keypad-text" style="color: {{ theme.text }}">0</text>
            </div>
            <div class="keypad-btn keypad-delete" onclick="onDelete" style="background-color: {{ theme.bg }}; border-color: {{ theme.border }}">
              <text class="keypad-text" style="color: #e74c3c">←</text>
            </div>
          </div>
        </div>

        <input class="verify-btn" type="button" value="验证激活码" onclick="doVerify" style="background-color: {{ theme.accent }}; color: {{ theme.bg }}" />

        <div class="result-box" show="{{ verifyResult }}" style="background-color: {{ theme.bg }}; border-color: {{ verifySuccess ? theme.accent : '#e74c3c' }}">
          <text class="result-label" style="color: {{ theme.textSecondary }}">验证结果</text>
          <text class="result-code" style="color: {{ verifySuccess ? theme.accent : '#e74c3c' }}">{{ verifyResult }}</text>
          <text class="result-detail" show="{{ verifyDetail }}" style="color: {{ theme.textSecondary }}">{{ verifyDetail }}</text>
        </div>
      </div>

      <text class="section-title" style="color: {{ theme.text }}">激活历史</text>
      <div class="history-list" style="background-color: {{ theme.card }}">
        <div for="{{ historyList }}" class="history-item">
          <div class="history-row">
            <div class="history-left">
              <text class="history-date" style="color: {{ theme.textSecondary }}">{{ $item.date }}</text>
              <text class="history-type" style="color: {{ theme.text }}">{{ $item.type }}</text>
              <text class="history-code" if="{{ $item.activationCode }}" style="font-size: 20px; color: {{ theme.textMuted }}">激活码: {{ $item.activationCode }}</text>
              <text class="history-code" if="{{ $item.redeemCode }}" style="font-size: 20px; color: {{ theme.textMuted }}">兑换码: {{ $item.redeemCode }}</text>
            </div>
            <div class="history-right">
              <text class="history-duration" style="color: {{ theme.accent }}">{{ $item.duration }}</text>
              <text class="history-status" style="color: {{ $item.status === '成功' ? '#27ae60' : '#e74c3c' }}">{{ $item.status }}</text>
            </div>
          </div>
          <div class="history-divider"></div>
        </div>
        <div class="history-empty" if="{{ historyList.length === 0 }}">
          <text class="history-empty-text" style="color: {{ theme.textMuted }}">暂无激活记录</text>
        </div>
      </div>

      <text class="section-title" style="color: {{ theme.text }}">设备信息</text>
      <div class="device-card" style="background-color: {{ theme.card }}">
        <div class="device-info-row">
          <text class="device-info-label" style="color: {{ theme.textSecondary }}">设备ID</text>
          <text class="device-info-value" style="color: {{ theme.accent }}">{{ deviceId || '获取中...' }}</text>
        </div>
        <div class="device-info-row">
          <text class="device-info-label" style="color: {{ theme.textSecondary }}">首次安装</text>
          <text class="device-info-value" style="color: {{ theme.text }}">{{ installTimeText }}</text>
        </div>
        <div class="device-info-row" if="{{ expireTimeText }}">
          <text class="device-info-label" style="color: {{ theme.textSecondary }}">到期时间</text>
          <text class="device-info-value" style="color: {{ theme.text }}">{{ expireTimeText }}</text>
        </div>
      </div>

      <div class="bottom-spacer"></div>
    </scroll>
  </div>
</template>

<script>
import router from "@system.router"
import prompt from "@system.prompt"
import vibrator from "@system.vibrator"
const store = require("../../data/store.js")
const authStore = require("../../data/auth-store.js")
const crypto = require("../../lib/crypto.js")

var ACTIVATION_URL = "https://app-auth.gudq.com/activate.html?deviceId="
var AFDIAN_URL = "https://www.ifdian.net/item/54100fe4a1e011f1a3f452540025c377"

function doVibrate() {
  try {
    vibrator.vibrate({ mode: "short" })
  } catch (e) {}
}

function formatDate(ts) {
  if (!ts) return ''
  var d = new Date(ts)
  var y = d.getFullYear()
  var m = String(d.getMonth() + 1)
  if (m.length < 2) m = '0' + m
  var day = String(d.getDate())
  if (day.length < 2) day = '0' + day
  return y + '-' + m + '-' + day
}

export default {
  private: {
    theme: {
      bg: '#1a1a2e',
      card: '#16213e',
      cardLight: '#0f3460',
      accent: '#7ec8e3',
      text: '#ffffff',
      textSecondary: '#888899',
      textMuted: '#555566',
      border: '#0f3460',
      borderLight: '#2a2a5a'
    },
    statusText: '加载中...',
    statusColor: '#888899',
    statusDetail: '',
    historyList: [],
    installTimeText: '',
    expireTimeText: '',
    afdianUrl: AFDIAN_URL,
    deviceId: '',
    qrText: '',
    cell0: null, cell1: null, cell2: null, cell3: null,
    cell4: null, cell5: null, cell6: null, cell7: null,
    cell8: null, cell9: null, cell10: null, cell11: null,
    cell12: null, cell13: null, cell14: null, cell15: null,
    cell16: null, cell17: null,
    cursor: 0,
    verifyResult: '',
    verifyDetail: '',
    verifySuccess: false
  },

  onInit() {
    var self = this
    store.getTheme(function(t) {
      self.theme = t
    })
    self.fetchDeviceId()
    self.loadStatus()
    self.loadHistory()
  },

  onShow() {
    var self = this
    store.getTheme(function(t) {
      self.theme = t
    })
  },

  loadStatus() {
    var self = this
    try {
      authStore.getFullStatus(function(status) {
        self.statusText = status.displayStatus || status.text
        self.statusColor = status.color

        if (status.isPermanent) {
          self.statusText = '高级版'
          self.statusColor = 'green'
        } else if (status.isActivated && status.isExpired) {
          self.statusText = '高级版'
          self.statusColor = 'red'
        } else if (status.isActivated && status.isExpiring) {
          self.statusText = '高级版'
          self.statusColor = '#e67e22'
        } else if (status.isActivated) {
          self.statusText = '高级版'
          self.statusColor = 'green'
        } else {
          self.statusText = '标准版'
          self.statusColor = 'gray'
        }

        self.statusDetail = status.expireText || ''

        if (status.installTime) {
          self.installTimeText = formatDate(status.installTime)
        }
        if (status.isPermanent) {
          self.expireTimeText = '永久有效'
        } else if (status.expireAt) {
          self.expireTimeText = formatDate(status.expireAt)
        } else {
          self.expireTimeText = ''
        }
      })
    } catch (e) {
      console.warn('[ACTIVATION] loadStatus error: ' + e)
    }
  },

  loadHistory() {
    var self = this
    try {
      authStore.getHistory(function(history) {
        self.historyList = history || []
      })
    } catch (e) {
      console.warn('[ACTIVATION] loadHistory error: ' + e)
    }
  },

  fetchDeviceId() {
    var self = this
    try {
      var device = require("@system.device")
      device.getDeviceId({
        success: function(data) {
          self.deviceId = data.deviceId || '未知'
          self.qrText = ACTIVATION_URL + self.deviceId
        },
        fail: function(data, code) {
          self.deviceId = '获取失败(' + code + ')'
          self.qrText = ACTIVATION_URL + 'unknown'
        }
      })
    } catch (e) {
      self.deviceId = '不可用'
      self.qrText = ACTIVATION_URL + 'demo'
    }
  },

  goBack() {
    router.back()
  },

  onCellClick(index) {
    doVibrate()
    this.cursor = index
  },

  onKeyPress(digit) {
    doVibrate()
    var idx = this.cursor
    this['cell' + idx] = digit
    if (this.cursor < 17) {
      this.cursor = this.cursor + 1
    }
  },

  onDelete() {
    doVibrate()
    var idx = this.cursor
    if (this['cell' + idx] === null || this['cell' + idx] === undefined) {
      if (this.cursor > 0) {
        this.cursor = this.cursor - 1
        idx = this.cursor
      }
    }
    this['cell' + idx] = null
    if (this.cursor > 0) {
      this.cursor = this.cursor - 1
    }
  },

  onClear() {
    doVibrate()
    for (var i = 0; i < 18; i++) {
      this['cell' + i] = null
    }
    this.cursor = 0
  },

  doVerify() {
    var code = ''
    for (var i = 0; i < 18; i++) {
      var val = this['cell' + i]
      if (val === null || val === undefined) {
        this.verifySuccess = false
        this.verifyResult = '请输入完整的18位激活码'
        this.verifyDetail = ''
        prompt.showToast({ message: '请输入完整的18位激活码' })
        return
      }
      code += val
    }

    var deviceId = this.deviceId

    if (!deviceId || deviceId.indexOf('获取失败') >= 0 || deviceId.indexOf('未知') >= 0 || deviceId.indexOf('不可用') >= 0) {
      this.verifySuccess = false
      this.verifyResult = '无法获取设备ID'
      this.verifyDetail = ''
      prompt.showToast({ message: '无法获取设备ID' })
      return
    }

    var self = this

    authStore.verifyMasterCode(code, deviceId, function(masterResult) {
      if (masterResult.success) {
        self.verifySuccess = true
        self.verifyResult = '激活成功 - ' + masterResult.duration + ' 天'
        self.verifyDetail = ''

        authStore.markActivated(masterResult.duration, code, '', function(success) {
          if (success) {
            prompt.showToast({ message: masterResult.message || '激活成功' })
            self.loadStatus()
            self.loadHistory()
          }
        })
        return
      }

      if (masterResult.blockReason === 'not_expired') {
        self.verifySuccess = false
        self.verifyResult = masterResult.reason
        self.verifyDetail = '您的授权仍在有效期内，无需使用万能码'
        prompt.showToast({ message: masterResult.reason })
        return
      }

      if (masterResult.blockReason === 'limit_reached') {
        self.verifySuccess = false
        self.verifyResult = masterResult.reason
        self.verifyDetail = ''
        prompt.showToast({ message: masterResult.reason })
        return
      }

      var decrypted = crypto.decryptActivationCode(code)
      if (!decrypted || !decrypted.valid) {
        self.verifySuccess = false
        self.verifyResult = decrypted ? decrypted.reason : '无效的激活码'
        self.verifyDetail = ''
        prompt.showToast({ message: '无效的激活码' })
        return
      }

      var deviceIdLast4 = deviceId.substring(deviceId.length - 4)
      if (decrypted.deviceId !== deviceIdLast4) {
        self.verifySuccess = false
        self.verifyResult = '设备ID不匹配'
        self.verifyDetail = '激活码设备ID: ' + decrypted.deviceId + ' | 本机设备ID后4位: ' + deviceIdLast4
        prompt.showToast({ message: '设备ID不匹配' })
        return
      }

      authStore.loadUsedRedeemCodes(function(usedRedeemList) {
        if (usedRedeemList.indexOf(decrypted.redeemCode) !== -1) {
          self.verifySuccess = false
          self.verifyResult = '兑换码已被使用'
          self.verifyDetail = '兑换码 ' + decrypted.redeemCode + ' 已被使用过，每个兑换码只能兑换一次'
          prompt.showToast({ message: '该兑换码已被使用' })
          return
        }

        var months = parseInt(decrypted.months, 10)

        var isPermanent = months === 99
        var days = isPermanent ? -1 : months * 30
        var durationLabel = isPermanent ? '永久' : (months + ' 个月')

        self.verifySuccess = true
        self.verifyResult = '激活成功 - ' + (isPermanent ? '永久高级版 ✓' : (months + ' 个月'))
        self.verifyDetail = isPermanent ? '' : ('有效期: ' + months + ' 个月')

        authStore.markActivated(days, code, decrypted.redeemCode, function(success) {
          if (success) {
            authStore.saveUsedCode(code)
            authStore.saveUsedRedeemCode(decrypted.redeemCode)
            prompt.showToast({ message: '激活成功，' + durationLabel })
            self.loadStatus()
            self.loadHistory()
          }
        })
      })
    })
  }
}
</script>

<style>
.page {
  flex-direction: column;
  padding: 44px 8px 8px 8px;
  min-height: 100%;
}

.page-scroll {
  flex: 1;
  flex-direction: column;
}

.back-header {
  flex-direction: row;
  align-items: center;
  padding: 0;
  margin-bottom: 10px;
}

.back-btn {
  width: 70px;
  height: 36px;
  border-radius: 8px;
  font-size: 14px;
  text-align: center;
}

.header-title {
  font-size: 18px;
  font-weight: bold;
  margin-left: 8px;
}

.status-card {
  flex-direction: column;
  border-radius: 12px;
  padding: 14px 12px;
  margin-bottom: 8px;
}

.status-row {
  flex-direction: row;
  align-items: center;
  margin-bottom: 4px;
}

.status-icon {
  font-size: 22px;
  margin-right: 8px;
}

.status-text {
  font-size: 18px;
  font-weight: bold;
}

.status-details {
  flex-direction: column;
  padding-left: 30px;
  margin-bottom: 6px;
}

.status-detail-line {
  font-size: 13px;
  margin-bottom: 2px;
}

.status-actions {
  flex-direction: row;
  padding-left: 30px;
}

.renew-btn {
  width: 80px;
  height: 32px;
  border-radius: 8px;
  font-size: 13px;
  font-weight: bold;
  text-align: center;
}

.section-title {
  font-size: 15px;
  font-weight: bold;
  margin-bottom: 6px;
  padding-left: 4px;
}

.history-list {
  flex-direction: column;
  border-radius: 12px;
  padding: 8px 12px;
  margin-bottom: 8px;
}

.history-item {
  flex-direction: column;
  padding: 8px 0;
}

.history-row {
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
}

.history-divider {
  height: 1px;
  background-color: rgba(255,255,255,0.05);
  margin-top: 8px;
  width: 100%;
}

.history-left {
  flex-direction: column;
}

.history-date {
  font-size: 12px;
  margin-bottom: 2px;
}

.history-type {
  font-size: 14px;
  font-weight: bold;
}

.history-right {
  flex-direction: column;
  align-items: flex-end;
}

.history-duration {
  font-size: 14px;
  font-weight: bold;
  margin-bottom: 2px;
}

.history-status {
  font-size: 12px;
}

.history-empty {
  padding: 16px;
  align-items: center;
}

.history-empty-text {
  font-size: 13px;
}

.device-card {
  flex-direction: column;
  border-radius: 12px;
  padding: 10px 12px;
  margin-bottom: 8px;
}

.device-info-row {
  flex-direction: row;
  justify-content: space-between;
  padding: 4px 0;
}

.device-info-label {
  font-size: 13px;
}

.device-info-value {
  font-size: 13px;
  font-weight: bold;
}

.step-card {
  flex-direction: column;
  border-radius: 12px;
  padding: 14px 12px;
  margin-bottom: 4px;
}

.step-header {
  flex-direction: row;
  align-items: center;
  margin-bottom: 6px;
}

.step-badge {
  width: 26px;
  height: 26px;
  border-radius: 13px;
  justify-content: center;
  align-items: center;
  margin-right: 8px;
}

.step-badge-text {
  font-size: 14px;
  font-weight: bold;
}

.step-title {
  font-size: 16px;
  font-weight: bold;
}

.step-desc {
  font-size: 12px;
  line-height: 18px;
  margin-bottom: 10px;
}

.step-arrow {
  font-size: 16px;
  text-align: center;
  margin-bottom: 4px;
}

.device-row {
  flex-direction: row;
  align-items: center;
  padding: 6px 0;
  margin-bottom: 6px;
}

.device-label {
  font-size: 13px;
  width: 72px;
}

.device-value {
  font-size: 15px;
  font-weight: bold;
}

.qr-section {
  flex-direction: column;
  align-items: center;
  margin-bottom: 6px;
}

.qr-wrapper {
  border-radius: 12px;
  padding: 8px;
}

.qr-label {
  font-size: 11px;
  margin-top: 6px;
}

.qr-loading {
  font-size: 13px;
  padding: 16px 0;
}

.url-box {
  border-radius: 6px;
  padding: 6px 10px;
  margin-bottom: 2px;
}

.url-text {
  font-size: 10px;
  word-break: break-all;
}

.cells-grid {
  flex-direction: column;
  border-radius: 12px;
  padding: 8px;
  margin-bottom: 8px;
}

.cells-row {
  flex-direction: row;
  justify-content: center;
}

.cell {
  width: 28px;
  height: 30px;
  border-radius: 6px;
  border-width: 1px;
  margin: 2px;
  justify-content: center;
  align-items: center;
}

.cell-active {
  border-width: 2px;
  border-color: #ff8c00;
}

.cell-text {
  font-size: 15px;
  font-weight: bold;
}

.keypad {
  flex-direction: column;
  border-radius: 12px;
  padding: 8px;
  margin-bottom: 8px;
}

.keypad-row {
  flex-direction: row;
  justify-content: center;
  margin-bottom: 4px;
}

.keypad-btn {
  width: 52px;
  height: 36px;
  border-radius: 8px;
  border-width: 1px;
  border-style: solid;
  justify-content: center;
  align-items: center;
  margin-left: 8px;
  margin-right: 8px;
}

.keypad-text {
  font-size: 18px;
  font-weight: bold;
}

.verify-btn {
  width: 100%;
  height: 40px;
  border-radius: 8px;
  font-size: 15px;
  text-align: center;
  align-self: center;
}

.result-box {
  flex-direction: column;
  border-radius: 8px;
  border-width: 2px;
  padding: 8px;
  margin-top: 8px;
}

.result-label {
  font-size: 12px;
  margin-bottom: 4px;
}

.result-code {
  font-size: 16px;
  font-weight: bold;
  margin-bottom: 4px;
}

.result-detail {
  font-size: 11px;
}

.bottom-spacer {
  height: 20px;
}

@media (shape: circle) {
  .page {
    padding: 44px 36px 44px 36px;
  }
  .back-header {
    padding: 0 20px;
    justify-content: center;
  }
}

@media (shape: capsule) {
  .page {
    padding: 44px 8px 12px 8px;
  }
}

@media (shape: rect) {
  .page {
    padding: 44px 6px 6px 6px;
  }
}
</style>