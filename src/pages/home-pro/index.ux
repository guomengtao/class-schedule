<template>
  <div class="schedule-page" style="background-color: {{ theme.bg }}">
    <div class="clock-row">
      <text class="clock-text" style="color: {{ theme.textSecondary }}">{{ currentTime }}</text>
    </div>

    <div class="header">
      <input class="nav-btn" type="button" value="&#9664;" onclick="prevDay" style="background-color: {{ theme.card }}; color: {{ theme.accent }}" />
      <text class="day-title" style="color: {{ theme.text }}">{{ currentDay }}</text>
      <div class="day-nav-btns">
        <div class="day-nav-circle" onclick="goToWeekView" style="background-color: {{ theme.cardLight }}">
          <text class="day-nav-text" style="color: {{ theme.accent }}">总</text>
        </div>
        <div class="day-nav-circle" onclick="goToToday" style="background-color: {{ theme.cardLight }}">
          <text class="day-nav-text" style="color: {{ theme.accent }}">今</text>
        </div>
        <div class="day-nav-circle" onclick="goToTomorrow" style="background-color: {{ theme.cardLight }}">
          <text class="day-nav-text" style="color: {{ theme.accent }}">明</text>
        </div>
      </div>
      <input class="nav-btn" type="button" value="&#9654;" onclick="nextDay" style="background-color: {{ theme.card }}; color: {{ theme.accent }}" />
    </div>

    <div class="custom-content-bar" if="{{ showCustomContent && customContent }}" style="background-color: {{ theme.cardLight }}">
      <div class="left-accent left-accent-4" style="background-color: {{ theme.accent }}"></div>
      <text class="custom-content-text" style="color: {{ theme.text }}">{{ customContent }}</text>
    </div>

    <div class="status-bar" style="background-color: {{ theme.cardLight }}">
      <div class="left-accent left-accent-6" style="background-color: {{ theme.accent }}"></div>
      <text class="status-tag" style="background-color: {{ theme.accent }}; color: {{ theme.bg }}">{{ statusTag }}</text>
      <text class="status-middle" style="color: {{ theme.text }}">{{ statusMainText }}</text>
      <text class="status-right" if="{{ statusTimeText }}" style="color: {{ theme.accent }}">{{ statusTimeText }}</text>
    </div>

    <div class="pinned-bar" if="{{ hasPinned }}" style="background-color: {{ theme.cardLight }}">
      <div class="left-accent left-accent-4" style="background-color: {{ theme.accent }}"></div>
      <div class="pinned-content">
        <div class="pinned-row">
          <div for="{{ pinnedPages }}" class="pinned-item" onclick="openPinnedPage($item.uri)" style="background-color: {{ theme.card }}">
            <text class="pinned-text" style="color: {{ theme.accent }}">{{ $item.name }}</text>
          </div>
        </div>
      </div>
    </div>

    <scroll class="class-list" scroll-y="{{true}}">
      <div for="{{ currentClasses }}" class="class-card-wrapper">
        <div class="class-card" style="background-color: {{ theme.card }}">
          <div class="left-accent left-accent-4" style="background-color: {{ theme.accent }}"></div>
          <div class="progress-bg" style="width: {{ $item.progress }}%; background-color: {{ $item.progressColor }}"></div>
          <div class="class-content">
            <div class="class-time">
              <text class="class-name" style="color: {{ theme.text }}">{{ $item.name }}</text>
              <text class="class-time-text" style="color: {{ theme.textSecondary }}">{{ $item.time }}</text>
            </div>
            <div class="class-info">
              <text class="class-location" if="{{ $item.location }}" style="color: {{ theme.accent }}">{{ $item.location }}</text>
              <text class="class-progress" if="{{ $item.progress >= 100 }}" style="color: {{ theme.textMuted }}">已结束</text>
              <text class="class-progress" if="{{ $item.progress > 0 && $item.progress < 100 }}" style="color: {{ theme.textMuted }}">{{ $item.progress }}%</text>
            </div>
          </div>
        </div>
      </div>

      <div if="{{ currentClasses.length === 0 }}" class="empty-card">
        <text class="empty-text" style="color: {{ theme.textMuted }}">今天没有课</text>
      </div>
    </scroll>

    <div class="quick-add" style="background-color: {{ theme.card }}">
      <text class="quick-add-title" onclick="toggleQuickAdd" style="color: {{ theme.accent }}">快速添加 {{ quickAddExpanded ? '▲' : '▼' }}</text>
      <scroll class="quick-add-scroll" scroll-x="{{true}}" if="{{ quickAddExpanded && quickCourseNames.length > 0 && !quickAddDisabled }}">
        <div class="quick-add-list">
          <div for="{{ quickCourseNames }}" class="quick-add-item" style="background-color: {{ theme.cardLight }}">
            <text class="quick-add-name" style="color: {{ theme.text }}">{{ $item }}</text>
          </div>
        </div>
      </scroll>
      <text class="quick-add-empty" if="{{ quickAddExpanded && quickAddDisabled }}" style="color: {{ theme.textMuted }}">今天课程已排满</text>
      <text class="quick-add-empty" if="{{ quickAddExpanded && !quickAddDisabled && quickCourseNames.length === 0 }}" style="color: {{ theme.textMuted }}">暂无已添加课程，请先添加课程</text>
    </div>

    <div class="bottom-buttons">
      <input class="add-btn" type="button" value="+ 添加课程" onclick="openAddCoursePage" style="background-color: {{ theme.accent }}; color: {{ theme.bg }}" />
      <input class="style-btn" type="button" value="设置" onclick="openSettings" style="background-color: {{ theme.cardLight }}; color: {{ theme.text }}" />
    </div>

    <div class="week-indicator" onclick="openScheduleManager">
      <text class="week-text" style="color: {{ theme.accent }}">⇄ {{ currentScheduleName }}</text>
    </div>
  </div>
</template>

<script>
import router from "@system.router"
const store = require("../../data/store.js")
const database = require("../../data/database.js")
var pinHelper = require("../../data/pin-helper.js")

var modules = {}
var moduleNames = [
  { key: "clock", text: "clock" },
  { key: "dayNav", text: "dayNav" },
  { key: "statusBar", text: "statusBar" },
  { key: "customContent", text: "customContent" },
  { key: "pinnedPages", text: "pinnedPages" },
  { key: "classList", text: "classList" },
  { key: "quickAdd", text: "quickAdd" },
  { key: "bottomButtons", text: "bottomButtons" },
  { key: "weekIndicator", text: "weekIndicator" }
]

function loadModule(key, mod) {
  if (mod && mod.init) {
    modules[key] = mod
  } else {
    modules[key] = null
  }
}

loadModule("clock", require("./modules/clock.js"))
loadModule("dayNav", require("./modules/day-nav.js"))
loadModule("statusBar", require("./modules/status-bar.js"))
loadModule("customContent", require("./modules/custom-content.js"))
loadModule("pinnedPages", require("./modules/pinned-pages.js"))
loadModule("classList", require("./modules/class-list.js"))
loadModule("quickAdd", require("./modules/quick-add.js"))
loadModule("bottomButtons", require("./modules/bottom-buttons.js"))
loadModule("weekIndicator", require("./modules/week-indicator.js"))

export default {
  private: {
    currentTime: "",
    currentDay: "Sunday",
    currentDayIndex: 0,
    statusTag: "",
    statusMainText: "",
    statusTimeText: "",
    showCustomContent: false,
    customContent: "",
    pinnedPages: [],
    hasPinned: false,
    currentClasses: [],
    schedule: [],
    quickAddExpanded: false,
    quickCourseNames: [],
    quickAddDisabled: false,
    quickAddTime: "",
    currentScheduleName: "Schedule 1",
    theme: {
      bg: '#1a1a2e',
      card: '#16213e',
      cardLight: '#0f3460',
      accent: '#7ec8e3',
      text: '#ffffff',
      textSecondary: '#888899',
      textMuted: '#555566',
      border: '#0f3460',
      borderLight: '#2a2a5a'
    }
  },

  onInit() {
    var self = this
    console.log("[home-pro] onInit start")

    store.getTheme(function(t) {
      self.theme = t
    })

    database.init(function() {
      database.getAllCourses(function(schedule) {
        self.schedule = schedule
        console.log("[home-pro] data loaded, " + schedule.length + " days")
        self.initAllModules()
      })
    })
  },

  initAllModules() {
    var self = this
    for (var i = 0; i < moduleNames.length; i++) {
      var m = moduleNames[i]
      var mod = modules[m.key]
      if (mod && mod.init) {
        try {
          mod.init(self)
          console.log("[home-pro] " + m.key + " init OK")
        } catch (e) {
          console.error("[home-pro] " + m.key + ".init failed: " + (e.message || e))
        }
      }
    }

    if (self.loadDayClasses) {
      self.loadDayClasses()
    }
    if (self.updateStatus) {
      self.updateStatus()
      if (self.startStatusTimer) self.startStatusTimer()
    }
    console.log("[home-pro] onInit done")
  },

  onDestroy() {
    if (this.stopClockTimer) this.stopClockTimer()
    if (this.stopStatusTimer) this.stopStatusTimer()
  },

  goBack() {
    router.back()
  },

  goToWeekView() {
    var storage = require("@system.storage")
    store.getCurrentScheduleIndex(function(idx) {
      storage.set({
        key: "weekview_index",
        value: String(idx),
        success: function() {
          router.push({ uri: "/pages/week-view" })
        },
        fail: function() {
          router.push({ uri: "/pages/week-view" })
        }
      })
    })
  },

  goToTomorrow() {
    var dayNames = ["星期日", "星期一", "星期二", "星期三", "星期四", "星期五", "星期六"]
    var todayIdx = new Date().getDay()
    var tomorrowIdx = todayIdx + 1
    if (tomorrowIdx >= dayNames.length) tomorrowIdx = 0
    if (this.currentDayIndex === tomorrowIdx) return
    this.currentDayIndex = tomorrowIdx
    this.currentDay = dayNames[tomorrowIdx]
    this.loadDayClasses()
    this.updateStatus()
  },

  pinToHome() {
    pinHelper.pinPage("Home Pro", "/pages/home-pro")
  }
}
</script>

<style>
.schedule-page {
  flex-direction: column;
  background-color: #1a1a2e;
  padding: 44px 8px 8px 8px;
  min-height: 100%;
}

.header {
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
  padding: 8px 4px;
  margin-bottom: 4px;
}

.clock-row {
  justify-content: center;
  align-items: center;
  height: 20px;
}

.clock-text {
  font-size: 18px;
  font-weight: normal;
  text-align: center;
}

.day-title {
  font-size: 20px;
  font-weight: bold;
  color: #e0e0e0;
  text-align: center;
}

.day-nav-btns {
  flex-direction: row;
  align-items: center;
}

.day-nav-circle {
  width: 32px;
  height: 32px;
  border-radius: 16px;
  justify-content: center;
  align-items: center;
  margin-left: 6px;
  background-color: #0f3460;
}

.day-nav-text {
  font-size: 20px;
  color: #7ec8e3;
}

.nav-btn {
  width: 48px;
  height: 48px;
  border-radius: 24px;
  font-size: 20px;
  text-align: center;
  justify-content: center;
  align-items: center;
}

.status-bar {
  flex-direction: row;
  align-items: center;
  border-radius: 14px;
  padding: 6px 14px;
  margin-bottom: 6px;
  height: 44px;
  overflow: hidden;
}

.status-tag {
  flex-shrink: 0;
  margin-right: 10px;
  padding: 2px 12px;
  border-radius: 12px;
  font-size: 14px;
  font-weight: bold;
}

.status-middle {
  flex: 1;
  font-size: 18px;
  max-lines: 1;
  text-overflow: ellipsis;
  overflow: hidden;
  min-width: 40px;
}

.status-right {
  flex-shrink: 0;
  font-size: 16px;
  font-weight: bold;
  margin-left: 10px;
  min-width: 50px;
  text-align: right;
}

.custom-content-bar {
  flex-direction: row;
  align-items: center;
  border-radius: 10px;
  padding: 8px 14px;
  margin-bottom: 6px;
}

.custom-content-text {
  font-size: 18px;
  flex: 1;
  text-align: center;
}

.pinned-bar {
  flex-direction: row;
  align-items: flex-start;
  border-radius: 10px;
  padding: 6px 14px;
  margin-bottom: 6px;
}

.pinned-content {
  flex: 1;
  flex-direction: column;
}

.pinned-hint {
  margin-bottom: 4px;
}

.pinned-row {
  flex-direction: row;
  flex-wrap: wrap;
}

.pinned-item {
  padding: 4px 12px;
  margin: 2px 6px 2px 0;
  border-radius: 8px;
}

.pinned-text {
  font-size: 13px;
}

.class-list {
  flex: 1;
  flex-direction: column;
  overflow: auto;
}

.class-card-wrapper {
  position: relative;
  overflow: hidden;
  border-radius: 10px;
  margin-bottom: 8px;
}

.class-card {
  position: relative;
  flex-direction: column;
  background-color: #16213e;
  border-radius: 10px;
  flex: 1;
  overflow: hidden;
}

.progress-bg {
  position: absolute;
  left: 0;
  top: 0;
  height: 100%;
  border-radius: 10px;
}

.left-accent {
  position: absolute;
  left: 0;
  top: 0;
  height: 100%;
}

.left-accent-6 {
  width: 6px;
}

.left-accent-4 {
  width: 4px;
}

.class-content {
  position: relative;
  flex-direction: column;
  width: 100%;
  padding: 14px;
}

.class-time {
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 6px;
}

.class-name {
  font-size: 28px;
  font-weight: bold;
  color: #ffffff;
  max-width: 140px;
}

.class-time-text {
  font-size: 28px;
  color: #a0a0b0;
}

.class-info {
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
}

.class-location {
  font-size: 28px;
  color: #7ec8e3;
}

.class-progress {
  font-size: 24px;
  color: #888899;
}

.empty-card {
  flex-direction: column;
  justify-content: center;
  align-items: center;
  padding: 40px;
}

.empty-text {
  font-size: 28px;
  color: #555566;
}

.quick-add {
  flex-direction: column;
  background-color: #16213e;
  border-radius: 10px;
  padding: 10px;
  margin-bottom: 8px;
}

.quick-add-title {
  font-size: 14px;
  font-weight: bold;
  color: #7ec8e3;
  margin-bottom: 6px;
  text-align: center;
}

.quick-add-scroll {
  height: 48px;
}

.quick-add-list {
  flex-direction: row;
}

.quick-add-item {
  padding: 6px 16px;
  margin-right: 8px;
  border-radius: 8px;
  background-color: #0f3460;
}

.quick-add-name {
  font-size: 24px;
  color: #ffffff;
}

.quick-add-empty {
  font-size: 20px;
  color: #555566;
  text-align: center;
  padding: 8px;
}

.bottom-buttons {
  flex-direction: row;
  justify-content: space-between;
}

.add-btn {
  width: 100px;
  height: 36px;
  border-radius: 8px;
  background-color: #7ec8e3;
  font-size: 14px;
  font-weight: bold;
  color: #1a1a2e;
  text-align: center;
}

.style-btn {
  width: 100px;
  height: 36px;
  border-radius: 8px;
  background-color: #0f3460;
  font-size: 14px;
  font-weight: bold;
  color: #ffffff;
  text-align: center;
}

.week-indicator {
  align-items: center;
  justify-content: center;
  padding: 8px 0;
}

.week-text {
  font-size: 20px;
  color: #7ec8e3;
  text-align: center;
}

@media (shape: circle) {
  .schedule-page {
    padding: 44px 36px 44px 36px;
  }
  .header {
    justify-content: center;
    padding: 8px 0;
  }
  .nav-btn {
    width: 40px;
    height: 40px;
    border-radius: 20px;
    font-size: 18px;
  }
  .day-title {
    font-size: 18px;
    margin: 0 8px;
  }
  .day-nav-btns {
    display: none;
  }
  .bottom-buttons {
    justify-content: center;
  }
  .add-btn {
    width: 80px;
    height: 32px;
    font-size: 13px;
    margin-right: 12px;
  }
  .style-btn {
    width: 80px;
    height: 32px;
    font-size: 13px;
  }
}

@media (shape: capsule) {
  .schedule-page {
    padding: 44px 8px 12px 8px;
  }
}

@media (shape: rect) {
  .schedule-page {
    padding: 44px 6px 6px 6px;
  }
}
</style>