<template>
  <div class="page" style="background-color: {{ theme.bg }}">
    <div class="header" style="background-color: {{ theme.bg }}">
      <div class="back-btn-wrapper" onclick="goBack" style="background-color: {{ theme.card }}">
        <image class="back-btn-icon" src="/common/icons/{{ iconTheme }}/icon_back.png"></image>
      </div>
      <text class="header-title" style="color: {{ theme.text }}">调休信息</text>
      <div class="header-placeholder"></div>
    </div>

    <scroll class="scroll-area" scroll-y="true">
      <div class="content-area">
        <div class="info-section" style="background-color: {{ theme.card }}">
          <text class="section-title" style="color: {{ theme.accent }}">规则一：对应挪用日（内置标准）</text>
          <text class="info-text" style="color: {{ theme.text }}">这是全国多数公办中小学沿用的规则，也是我们内置 targetWeekDay 的设定依据。</text>
          <text class="info-em" style="color: {{ theme.textSecondary }}">核心逻辑：假期里占用了星期几的工作日，调休那天就补星期几的课。</text>
          <text class="info-text" style="color: {{ theme.text }}">假期之前的调休 → 补假期开头第一个工作日的课</text>
          <text class="info-text" style="color: {{ theme.text }}">假期之后的调休 → 补假期最后一个工作日的课</text>
          <text class="info-em" style="color: {{ theme.textSecondary }}">示例：2026年春节占用了周一和周五</text>
          <text class="info-text" style="color: {{ theme.text }}">2月14日（周六）补班 → 上周一的课</text>
          <text class="info-text" style="color: {{ theme.text }}">2月28日（周六）补班 → 上周五的课</text>
        </div>

        <div class="info-section" style="background-color: {{ theme.card }}">
          <text class="section-title" style="color: {{ theme.accent }}">规则二：周末顺推</text>
          <text class="info-text" style="color: {{ theme.text }}">很多中小学为了降低记忆成本，执行更简单的固定规则：</text>
          <text class="info-text" style="color: {{ theme.text }}">星期六调休上班 → 一律上周一的课</text>
          <text class="info-text" style="color: {{ theme.text }}">星期日调休上班 → 一律上周二的课</text>
          <text class="info-text" style="color: {{ theme.textSecondary }}">规则简单好记，在区县、乡镇级学校很普遍。</text>
        </div>

        <div class="info-section" style="background-color: {{ theme.card }}">
          <text class="section-title" style="color: {{ theme.accent }}">补充说明</text>
          <text class="info-text" style="color: {{ theme.text }}">大学无统一标准：高校自主权高，很多不执行调休补课。</text>
          <text class="info-text" style="color: {{ theme.text }}">地方差异普遍存在：部分地区有地方性规定，如周六统一补周五的课。</text>
        </div>

        <div class="info-section" style="background-color: {{ theme.card }}">
          <text class="section-title" style="color: {{ theme.accent }}">产品设计</text>
          <text class="info-text" style="color: {{ theme.text }}">内置采用「对应挪用日」规则，覆盖最多数公办中小学用户。</text>
          <text class="info-text" style="color: {{ theme.text }}">自定义优先：正因为全国无统一标准，支持用自定义日期覆盖内置映射，适配不同学校的特殊安排。</text>
          <text class="info-text" style="color: {{ theme.text }}">首页表现：调休日显示「今天调休，按XX课表」并按 targetWeekDay 自动加载对应课程；假期则展示祝福卡片。</text>
        </div>

        <div class="info-section" style="background-color: {{ theme.card }}">
          <text class="section-title" style="color: {{ theme.accent }}">官方来源</text>
          <text class="info-text" style="color: {{ theme.text }}">《国务院办公厅关于2026年部分节假日安排的通知》</text>
          <text class="info-text" style="color: {{ theme.textSecondary }}">国办发明电〔2025〕7号 · 2025年11月4日发布</text>
          <text class="info-text" style="color: {{ theme.textSecondary }}">索引号：000014349/2025-00087 · 中国政府网</text>
        </div>

        <div class="info-section" style="background-color: {{ theme.card }}">
          <text class="section-title" style="color: {{ theme.accent }}">胶囊屏适配</text>
          <text class="info-text" style="color: {{ theme.textSecondary }}">优先适配胶囊屏（shape: capsule），192px 窄屏上自动精简布局，字号和间距按媒体查询缩放，确保舒适阅读。</text>
        </div>
      </div>
    </scroll>
  </div>
</template>

<script>
import router from "@system.router"
const store = require("../../data/store.js")

export default {
  private: {
    iconTheme: "dark",
    theme: {}
  },

  onInit() {
    var self = this
    store.getTheme(function(t, themeName) {
      self.theme = t
      self.iconTheme = (themeName === 'light' || themeName === 'warm') ? 'light' : 'dark'
    })
  },

  onShow() {
    var self = this
    store.getTheme(function(t, themeName) {
      self.theme = t
      self.iconTheme = (themeName === 'light' || themeName === 'warm') ? 'light' : 'dark'
    }, true)
  },

  goBack() {
    router.back()
  }
}
</script>

<style>
.page {
  width: 100%;
  height: 100%;
  flex-direction: column;
}

/* ── 胶囊屏优先（默认） ── */
.header {
  flex-direction: row;
  align-items: center;
  padding: 8px 6px 6px 6px;
  flex-shrink: 0;
}

.back-btn-wrapper {
  width: 36px;
  height: 36px;
  border-radius: 18px;
  justify-content: center;
  align-items: center;
  flex-shrink: 0;
}

.back-btn-icon {
  width: 20px;
  height: 20px;
}

.header-title {
  font-size: 18px;
  font-weight: bold;
  text-align: center;
  flex: 1;
  lines: 1;
  text-overflow: ellipsis;
}

.header-placeholder {
  width: 36px;
  flex-shrink: 0;
}

.scroll-area {
  flex: 1;
  width: 100%;
  flex-direction: column;
}

.content-area {
  width: 100%;
  flex-direction: column;
  padding: 4px 8px 32px 8px;
}

.info-section {
  flex-direction: column;
  padding: 14px 12px;
  margin-bottom: 14px;
}

.section-title {
  font-size: 18px;
  font-weight: bold;
  margin-bottom: 10px;
}

.info-text {
  font-size: 17px;
  line-height: 26px;
  margin-bottom: 8px;
}

.info-em {
  font-size: 17px;
  line-height: 26px;
  margin-bottom: 8px;
}

/* ── 方屏/大屏 ── */
@media (shape: rect) {
  .header {
    padding: 10px 12px 8px 12px;
  }

  .back-btn-wrapper {
    width: 40px;
    height: 40px;
    border-radius: 20px;
  }

  .back-btn-icon {
    width: 22px;
    height: 22px;
  }

  .header-title {
    font-size: 20px;
  }

  .header-placeholder {
    width: 40px;
  }

  .content-area {
    padding: 6px 14px 40px 14px;
  }

  .info-section {
    padding: 18px;
    margin-bottom: 18px;
  }

  .section-title {
    font-size: 20px;
    margin-bottom: 12px;
  }

  .info-text {
    font-size: 18px;
    line-height: 30px;
    margin-bottom: 10px;
  }

  .info-em {
    font-size: 18px;
    line-height: 28px;
    margin-bottom: 10px;
  }
}

/* ── 圆形屏 ── */
@media (shape: circle) {
  .header {
    padding: 14px 16px 8px 16px;
  }

  .back-btn-wrapper {
    width: 36px;
    height: 36px;
    border-radius: 18px;
  }

  .back-btn-icon {
    width: 20px;
    height: 20px;
  }

  .header-title {
    font-size: 18px;
  }

  .header-placeholder {
    width: 36px;
  }

  .content-area {
    padding: 4px 12px 28px 12px;
  }

  .info-section {
    padding: 12px;
    margin-bottom: 12px;
  }

  .section-title {
    font-size: 17px;
    margin-bottom: 8px;
  }

  .info-text {
    font-size: 16px;
    line-height: 24px;
    margin-bottom: 6px;
  }

  .info-em {
    font-size: 16px;
    line-height: 24px;
    margin-bottom: 6px;
  }
}
</style>