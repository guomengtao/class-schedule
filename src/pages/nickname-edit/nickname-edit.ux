<template>
  <div class="edit-page" style="background-color: {{ theme.bg }}">
    <div class="back-header">
      <text class="back-btn" onclick="goBack" style="background-color: {{ theme.card }}; color: {{ theme.accent }}">◀</text>
      <text class="header-title" style="color: {{ theme.text }}">编辑昵称</text>
      <input class="save-btn" type="button" value="保存" onclick="saveNickname" style="background-color: {{ theme.accent }}; color: {{ theme.bg }}" />
    </div>

    <div class="edit-content">
      <div class="nickname-display" onclick="openInput" style="background-color: {{ theme.card }}; border-color: {{ theme.accent }}">
        <text class="nickname-text" style="color: {{ theme.text }}">{{ currentName || '输入昵称' }}</text>
      </div>
    </div>
  </div>
</template>

<script>
import router from "@system.router"
import prompt from "@system.prompt"
const store = require("../../data/store.js")

export default {
  private: {
    currentName: "",
    savedName: "",
    theme: {
      bg: '#1a1a2e',
      card: '#16213e',
      accent: '#7ec8e3',
      text: '#ffffff',
      textSecondary: '#888899',
      textMuted: '#555566'
    }
  },

  onInit() {
    var self = this
    store.getTheme(function(t) {
      self.theme = t
    })
    store.getNickname(function(name) {
      self.savedName = name || ""
      self.currentName = self.savedName
    })
  },

  onShow() {
    var self = this
    store.getTheme(function(t) {
      self.theme = t
    })
    var storage = require("@system.storage")
    storage.get({
      key: "chinese_input_result",
      success: function(data) {
        if (data !== undefined && data !== null && data !== "") {
          self.currentName = data
          storage.delete({ key: "chinese_input_result", fail: function() {} })
        }
      },
      fail: function() {}
    })
  },

  openInput() {
    var storage = require("@system.storage")
    storage.set({ key: "chinese_input_title", value: "编辑昵称", success: function() {} })
    storage.set({ key: "chinese_input_placeholder", value: "输入昵称", success: function() {} })
    storage.set({ key: "chinese_input_value", value: this.currentName, success: function() {} })
    storage.set({ key: "chinese_input_maxlen", value: "5", success: function() {} })
    storage.set({ key: "chinese_input_return_key", value: "chinese_input_result", success: function() {
      router.push({ uri: "/pages/chinese-input" })
    }})
  },

  saveNickname() {
    var newName = this.currentName.trim()
    if (!newName) {
      prompt.showToast({ message: "昵称不能为空" })
      return
    }
    var self = this
    store.setNickname(newName, function() {
      self.savedName = newName
      prompt.showToast({ message: "昵称已保存" })
      router.back()
    })
  },

  goBack() {
    router.back()
  }
}
</script>

<style>
.edit-page {
  flex-direction: column;
  padding: 38px 8px 10px 8px;
  width: 100%;
  height: 100%;
}

.back-header {
  flex-direction: row;
  align-items: center;
  height: 40px;
  margin-bottom: 14px;
}

.back-btn {
  width: 40px;
  height: 30px;
  border-radius: 15px;
  font-size: 20px;
  text-align: center;
  line-height: 30px;
  flex-shrink: 0;
}

.header-title {
  font-size: 26px;
  font-weight: bold;
  flex: 1;
  text-align: center;
  margin-right: 4px;
  line-height: 34px;
  lines: 1;
}

.save-btn {
  width: 56px;
  height: 30px;
  border-radius: 15px;
  font-size: 22px;
  font-weight: bold;
  text-align: center;
  line-height: 28px;
  flex-shrink: 0;
}

.edit-content {
  flex-direction: column;
  align-items: center;
  padding: 14px 4px 0 4px;
}

.nickname-display {
  width: 100%;
  height: 56px;
  border-radius: 12px;
  border-width: 2px;
  justify-content: center;
  align-items: center;
  flex-direction: row;
}

.nickname-text {
  font-size: 28px;
  lines: 1;
  text-overflow: ellipsis;
  max-width: 80%;
  line-height: 36px;
}

@media (shape: circle) {
  .edit-page {
    padding: 44px 36px 44px 36px;
  }
  .back-header {
    height: 44px;
    margin-bottom: 16px;
  }
  .back-btn {
    width: 44px;
    height: 34px;
    font-size: 22px;
    line-height: 34px;
  }
  .header-title {
    font-size: 28px;
    line-height: 38px;
  }
  .save-btn {
    width: 60px;
    height: 34px;
    font-size: 24px;
    line-height: 32px;
  }
  .nickname-display {
    height: 60px;
  }
  .nickname-text {
    font-size: 32px;
    line-height: 40px;
  }
}

@media (shape: rect) {
  .edit-page {
    padding: 44px 12px 16px 12px;
  }
  .back-header {
    height: 44px;
    margin-bottom: 16px;
  }
  .back-btn {
    width: 44px;
    height: 34px;
    font-size: 22px;
    line-height: 34px;
  }
  .header-title {
    font-size: 30px;
    line-height: 40px;
  }
  .save-btn {
    width: 64px;
    height: 34px;
    font-size: 24px;
    line-height: 32px;
  }
  .nickname-display {
    height: 64px;
  }
  .nickname-text {
    font-size: 30px;
    line-height: 40px;
  }
}
</style>